# 弱全自动化改造方案

## 1. 文档目的

本文档用于定义当前项目迈向“弱全自动”阶段的改造方案。

这里的“弱全自动”特指：

1. 用户仍然提供 JD，并在必要时手动登录猎聘。
2. 系统自动完成岗位分析、搜索词生成、猎聘搜索执行、结果页遍历、候选人详情抓取、批量匹配与结果导出。
3. 系统不追求完全无人值守，也不追求自动登录、自动绕过平台风控、自动沟通候选人。

本文档重点解决两个当前最关键的难点：

1. 如何让 AI 输出“可直接用于猎聘搜索”的关键词，而不是大而全、无法落地的职位名。
2. 如何在猎聘找人页顶部存在大量输入框和按钮的情况下，稳定识别真正可操作的主搜索输入框与搜索按钮。

补充说明：

1. 本文档是弱全自动阶段的总体方案文档。
2. 与猎聘找人页真实结构和交互细节对应的事实依据，已经单独沉淀在：
   1. `猎聘找人界面真实元素映射说明.md`
   2. `继续开发交接说明-猎聘找人自动化.md`
3. 当本方案中的抽象设计与真实页面调试结果冲突时，以真实页面映射文档为准，并及时回写本方案。

除非后续明确改变方向，否则弱全自动阶段的设计、实施和验收都以本文档为基线。

## 2. 当前现状与问题定义

当前项目已经具备以下基础：

1. `岗位分析` 已经能输出结构化 HTML 报告。
2. `search_strategy_service.py` 已经可以从 HTML 中解析一批关键词。
3. `liepin_browser.py` 已经具备持久化浏览器和登录态复用能力。
4. `liepin_search_service.py` 已经具备结果页解析、翻页、详情页打开等能力。
5. `liepin_search_task_service.py` 已经具备候选人抓取入 Excel 的能力。
6. `batch_match_service.py` 已经具备批量匹配和回写能力。

但是，真正从“半自动”走向“弱全自动”，目前还卡在两个核心瓶颈上。

### 2.1 难点一：AI 产出的搜索词不够可执行

当前岗位分析倾向于输出“岗位名称级”词，例如：

1. `灯具结构工程师`
2. `结构工程师`
3. `照明结构设计工程师`

这些词看上去专业，但在猎聘实际搜索里经常有两个问题：

1. 过长，导致召回太窄。
2. 太像标准职位名，而不是候选人简历中真实高频出现的短词组合。

实际搜索中，顾问更常用的是：

1. 一个能力短词，例如 `结构`
2. 一个领域短词，例如 `灯具`
3. 视情况补一个业务或专业短词，例如 `散热`、`注塑`、`钣金`、`LED`

也就是说，搜索词生成的目标不应是“生成职位全称”，而应是“生成最短、最有判别力、最贴近简历真实写法的关键词组合单元”。

### 2.2 难点二：猎聘找人页顶部输入框和按钮太多

当前项目在 `liepin_search_service.py` 中已经存在多组 `SEARCH_INPUT_SELECTORS` 和 `SEARCH_BUTTON_SELECTORS`，说明页面并不稳定，且顶部可见输入框很多。

问题本质不是“selector 数量不够”，而是当前页面存在以下复杂性：

1. 顶部导航区、筛选区、搜索区可能都有输入框。
2. 页面上可能存在多个 class 相似的输入框。
3. 搜索按钮、筛选按钮、提交按钮都可能共用类似样式。
4. 有些元素看起来可见，但不是当前主搜索框。
5. 页面更新后类名可能变化，但交互结构通常还在。

因此，不能单纯靠“再补几个 selector”解决，而要建立一套稳定的“主搜索控件识别链路”。

## 3. 弱全自动阶段目标

弱全自动阶段的目标定义如下：

1. 用户输入 JD。
2. 系统自动完成岗位分析。
3. 系统自动将岗位分析结果转换为“可执行搜索计划”。
4. 用户确认搜索计划或选择系统推荐计划。
5. 系统自动打开猎聘找人页。
6. 系统自动识别主搜索输入框和搜索按钮。
7. 系统自动按搜索轮次执行关键词组合搜索。
8. 系统自动遍历结果页并抓取候选人。
9. 系统自动抓取详情并落 Excel。
10. 系统自动执行批量匹配并回写 Excel。

本阶段明确不做：

1. 不自动登录猎聘。
2. 不自动绕过猎聘风控。
3. 不自动进行候选人沟通、投递或外呼。
4. 不追求搜索词完全由模型自由发挥。

## 4. 设计原则

弱全自动阶段遵循以下原则：

1. 搜索词优先服务于“搜索命中率”，而不是服务于“分析报告可读性”。
2. 搜索词生成必须贴近猎聘候选人简历真实写法，不追求职位命名完整。
3. 搜索策略必须是“短词组合 + 分轮执行”，而不是“大职位词一次搜完”。
4. 页面控件定位优先依据“页面结构和交互角色”，而不是堆更多脆弱 selector。
5. 所有自动化都必须支持调试、回放、失败重试和人工接管。
6. 先把“稳定执行一条链路”做好，再追求完全无人值守。
7. 筛选字段必须按真实交互类型分流实现，不能把所有字段都当成“点击后立即生效”的同类元素。
8. 每次筛选后都必须等待页面完成自动刷新并恢复稳定，不能连续无脑点击多个字段。

## 4.1 基于真实页面调试的实现修正

经过对当前线上猎聘 `找简历` 页面做真实浏览器调试后，已经确认弱全自动实现不能停留在“找到输入框和按钮”这一层，而必须吸收以下页面级约束：

1. 页面上的筛选字段至少分为四类：
   1. 点击即生效型标签筛选
   2. 下拉型筛选
   3. 弹窗确认型筛选
   4. 区间输入型筛选
2. 不同类型字段的自动化实现方式完全不同。
3. 后续 `liepin_search_service.py` 的实现不能只靠通用 selector，需要引入字段类型分派和统一等待机制。

已确认的真实页面样板如下：

1. 点击即生效型标签筛选：
   1. `工作年限`
   2. 点击后立即切换选中态
   3. 自动触发搜索
   4. 页面进入 loading
2. 弹窗确认型筛选：
   1. `目前城市 -> 其他`
   2. 点击后打开城市弹窗
   3. 选择城市后必须点 `确认`
   4. 之后页面才会应用筛选并刷新
3. 下拉型筛选：
   1. `性别`
   2. 更稳定的操作方式是 focus 内部 input 后使用键盘 `ArrowDown/Enter`
4. 区间输入型筛选：
   1. `年龄`
   2. 由两个 `age-input` 组成区间输入，不是下拉

实现含义：

1. 城市 `其他` 不能当普通标签点选。
2. 性别不能假设点击外层容器就一定展开下拉。
3. 年龄不能沿用性别/活跃度的下拉逻辑。
4. 页面刷新期间必须禁止继续执行后续筛选动作。

## 5. 总体方案概览

弱全自动阶段建议新增一层“可执行搜索计划”。

当前链路：

1. JD
2. 岗位分析 HTML
3. 从 HTML 提取搜索词
4. 直接拿搜索词去搜

建议改为：

1. JD
2. 岗位分析 HTML + 匹配标准
3. 关键词拆解与归一化
4. 生成“搜索原子词池”
5. 生成“分轮组合搜索计划”
6. 在猎聘中自动执行搜索计划

也就是说，弱全自动阶段不应该直接消费 `第一轮精准搜索词` 这几个展示词，而应该多做一步：

把分析结果转成“原子词”和“组合规则”。

## 6. 难点一方案：可执行搜索词生成机制

## 6.1 核心判断

当前问题不是模型不懂岗位，而是模型输出层级不对。

它现在更像在输出：

1. 岗位名称
2. 同义职位名
3. 行业术语

而真正执行猎聘搜索时，我们需要的是：

1. 领域词
2. 能力词
3. 工艺词
4. 产品词
5. 材料/结构对象词
6. 可选约束词

举例：`灯具结构工程师`

真正可搜索的高价值拆分可能是：

1. 领域词：`灯具`、`照明`、`LED`
2. 能力词：`结构`
3. 工艺词：`散热`、`注塑`、`钣金`、`压铸`
4. 对象词：`外壳`、`支架`、`模组`

而不是只输出：

1. `灯具结构工程师`
2. `照明结构工程师`
3. `结构工程师`

## 6.2 新的搜索词输出目标

岗位分析后的搜索策略，不再只追求“给用户看的一堆关键词”，而要落成以下结构：

```text
SearchIntent
- domain_terms
- capability_terms
- process_terms
- object_terms
- exclude_terms
- optional_constraint_terms
- search_rounds
```

字段说明：

1. `domain_terms`
   用来限定行业或产品领域，如 `灯具`、`照明`、`LED`。
2. `capability_terms`
   用来限定核心能力，如 `结构`、`结构设计`。
3. `process_terms`
   用来限定工艺与方法，如 `散热`、`注塑`、`钣金`。
4. `object_terms`
   用来限定具体对象，如 `外壳`、`支架`、`模组`。
5. `exclude_terms`
   用来排除高噪音方向。
6. `optional_constraint_terms`
   用来做二次收敛，如专业软件、材料、认证体系词。
7. `search_rounds`
   用来定义最终实际执行的搜索组合。

## 6.3 关键词生成的三层模型

建议把搜索词生成分成三层，而不是让模型一步到位吐“最终搜索词”。

### 第一层：岗位语义拆解

目标：先识别这个岗位真正的搜索维度。

例如对 `灯具结构工程师`，系统应识别：

1. 主能力维度：结构
2. 产品领域维度：灯具 / 照明 / LED
3. 工艺维度：散热 / 注塑 / 钣金 / 压铸
4. 常见对象维度：灯体 / 外壳 / 支架 / 模组

这一步不要急着生成完整搜索串，只做语义拆解。

### 第二层：短词归一化

目标：把拆解出的候选词压缩成“短、常见、可被简历直接命中”的词。

规则：

1. 优先保留 2-4 字的高频短词。
2. 优先保留简历里更常出现的写法，而不是正式岗位名。
3. 职位后缀如 `工程师`、`经理`、`专员` 默认弱化处理。
4. 超过 6 个字的词默认不作为首轮搜索词。
5. 如一个短词与职位名重合度高但过宽，需要和领域词配对使用。

例如：

1. `结构工程师` -> 保留 `结构`
2. `灯具结构工程师` -> 拆为 `灯具` + `结构`
3. `照明产品结构设计` -> 拆为 `照明` + `结构`

### 第三层：组合成搜索轮次

目标：生成真正执行的搜索计划。

建议分三轮：

1. 第一轮：主能力词 + 主领域词
2. 第二轮：主能力词 + 替代领域词
3. 第三轮：主能力词 + 领域词 + 工艺词

例如：

1. `结构 灯具`
2. `结构 照明`
3. `结构 LED`
4. `结构 灯具 散热`
5. `结构 灯具 注塑`
6. `结构 照明 钣金`

这类组合通常比 `灯具结构工程师` 更容易命中正确人群。

## 6.4 搜索词生成规则

建议新增一套硬规则，不把搜索词质量完全交给模型自由发挥。

### 规则 1：禁止把完整职位名直接当首轮唯一关键词

例如以下词默认不应作为首轮唯一搜索词：

1. `灯具结构工程师`
2. `高级灯具结构设计工程师`
3. `照明产品结构工程师`

它们可以保留为“补充搜索词”或“标签解释词”，但不应成为主执行词。

### 规则 2：优先输出“一个能力词 + 一个领域词”

优先级最高的搜索组合模板应为：

1. `能力词 + 领域词`
2. `能力词 + 替代领域词`

例如：

1. `结构 灯具`
2. `结构 照明`
3. `结构 LED`

### 规则 3：第三个词只在需要收敛噪音时补充

不要一开始就把 3-4 个词全塞进去。

第三个词适合以下场景：

1. 第一轮结果太多，噪音高。
2. 核心工艺是岗位强要求。
3. 领域词本身过泛。

例如：

1. `结构 灯具 散热`
2. `结构 灯具 注塑`

### 规则 4：职位后缀默认不作为主检索词

例如：

1. `工程师`
2. `设计师`
3. `经理`
4. `主管`

这些词单独价值低，通常不应成为核心原子词。

### 规则 5：优先输出简历会写的词，而不是招聘方会写的词

例如招聘方写：

1. `负责照明产品结构开发`

候选人简历更可能写：

1. `灯具`
2. `结构`
3. `散热`
4. `钣金`
5. `注塑`

所以系统必须偏向候选人视角，而不是 JD 视角。

## 6.5 建议新增的搜索策略数据结构

建议把当前 `SearchStrategy` 扩展为两层：

```text
SearchStrategy
- precise_keywords
- expansion_keywords
- synonyms
- exclude_keywords
- boolean_queries
- source_company_hints
- atomic_terms
- executable_rounds
```

其中：

1. `atomic_terms`
   保存拆解后的原子词池。
2. `executable_rounds`
   保存最终要自动执行的轮次组合。

建议 `executable_rounds` 结构如下：

```json
[
  {
    "label": "第一轮主搜",
    "query": "结构 灯具",
    "intent": "能力+主领域",
    "priority": 1
  },
  {
    "label": "第二轮扩展",
    "query": "结构 照明",
    "intent": "能力+替代领域",
    "priority": 2
  },
  {
    "label": "第三轮收敛",
    "query": "结构 灯具 散热",
    "intent": "能力+领域+关键工艺",
    "priority": 3
  }
]
```

## 6.6 模型与规则的分工

弱全自动阶段建议采用“模型提建议，规则做裁剪”的方式。

模型负责：

1. 识别岗位语义。
2. 给出候选词池。
3. 判断哪些词更贴近简历高频写法。
4. 提供领域替代词。

规则负责：

1. 过滤过长词。
2. 过滤职位后缀词。
3. 限制首轮必须是短词组合。
4. 生成固定轮次模板。
5. 保证输出一定能执行。

这是必要的，因为“搜索词是否好用”是一个强约束问题，不能只靠模型自由写作。

## 6.7 针对当前项目的改造建议

建议不要继续只从 HTML 的 `tag-cloud` 里被动提词，而是新增一个专门的“可执行搜索词生成”步骤。

推荐改造方向：

1. 保留当前 HTML 分析结果，用于展示。
2. 新增一段结构化搜索 JSON 输出，不面向展示，只面向执行。
3. `SearchStrategyService` 优先解析结构化 JSON。
4. 若 JSON 解析失败，再回退到当前 HTML tag 提取方式。

建议模型在岗位分析结束后额外输出：

```json
{
  "domain_terms": ["灯具", "照明", "LED"],
  "capability_terms": ["结构", "结构设计"],
  "process_terms": ["散热", "注塑", "钣金"],
  "object_terms": ["外壳", "支架", "模组"],
  "exclude_terms": ["建筑结构", "钢结构"],
  "recommended_rounds": [
    "结构 灯具",
    "结构 照明",
    "结构 LED",
    "结构 灯具 散热"
  ]
}
```

然后由程序做二次清洗与裁剪。

## 7. 难点二方案：猎聘主输入框与按钮稳定定位

## 7.1 核心判断

当前问题不是“找不到输入框”，而是“页面上有太多看起来像输入框的元素”。

因此解决思路不应是单一 selector，而应是多阶段识别：

1. 先锁定正确的搜索区域。
2. 再从区域内找到最可能的主输入框。
3. 再找到与该输入框关联的搜索按钮。
4. 最后用真实交互验证该组合是否有效。

## 7.2 建议采用“区域优先”的控件识别逻辑

建议将输入框识别分为四步。

### 第一步：识别搜索主区域

不要直接在整页寻找输入框，而是先找最可能的搜索工具条或搜索头部区域。

候选依据：

1. 区域内同时存在输入框和带 `搜索`、`找人` 文案的按钮。
2. 区域靠近页面顶部且宽度足够大。
3. 区域在点击搜索后会触发结果变化。
4. 区域周围有 `关键词`、`搜索`、`找人` 等语义文本。

也就是说，先识别一个“搜索容器”，再在容器内找控件。

### 第二步：在主区域内筛输入框

对候选输入框做打分，而不是命中即用。

建议评分维度：

1. placeholder 是否包含 `搜索`、`关键词`、`职位`、`人选`。
2. 是否可见、可编辑、可 focus。
3. 宽度是否足够大。
4. 所在容器是否靠近搜索按钮。
5. 输入后是否能读取回填值。
6. 输入框是否位于主搜索区域首行。

最终选择得分最高的输入框作为主输入框。

### 第三步：绑定对应搜索按钮

按钮不要全页找，而应从主输入框向上回溯最近容器，再在容器内找按钮。

候选按钮打分维度：

1. 文案是否为 `搜索`、`找人`。
2. 与输入框的距离是否最近。
3. 是否是可点击按钮元素。
4. 点击后是否引发 URL 或结果页变化。

### 第四步：交互验证

真正的主输入框和按钮，必须经过一次轻量验证。

验证方式：

1. 写入测试关键词。
2. 触发按钮或回车。
3. 检查结果页是否变化。
4. 变化成功则缓存该控件路径。

这一步很关键，它比 selector 本身更可靠。

补充：

基于真实页面调试，当前页面的主搜索控件已经有明确的高优先级定位依据：

1. 主按钮：`button.search-btn`
2. 主搜索容器：`div.search-auto-complete-box`
3. 主搜索输入框：
   1. 位于主按钮左侧
   2. 宽度约 `680`
   3. `input.ant-select-selection-search-input`

因此：

1. 第一版实现不需要先上完整探针系统再开工。
2. 可以先把这套已验证结构写进高优先级定位逻辑。
3. 探针机制作为后续页面变动时的增强和兜底。

## 7.3 建议新增“页面结构探针”机制

当前项目已经有 debug snapshot 能力，但缺少“控件结构探针”。

建议新增一个页面探针功能，专门分析猎聘找人页顶部区域，输出：

1. 页面上所有可见输入框
2. 它们的 placeholder
3. 它们所在容器层级
4. 宽高与坐标
5. 容器内相邻按钮文案
6. 候选得分

输出示例：

```text
候选输入框 #1
- placeholder: 请输入关键词
- rect: x=320 y=110 w=420 h=36
- container buttons: 搜索, 重置
- score: 92

候选输入框 #2
- placeholder: 公司名称
- rect: x=320 y=180 w=200 h=32
- container buttons: 不限
- score: 31
```

这个工具的价值在于：

1. 页面变了时可以快速看出识别为什么失败。
2. 不需要每次靠肉眼读整份 HTML 结构。
3. 能为 selector 调整提供直接证据。

## 7.4 建议采用 DOM 打分而不是纯 selector 方案

建议在 `liepin_search_service.py` 中新增一段 JavaScript 探测逻辑，和当前 DOM fallback 抓候选人思路类似。

它不是直接找某个类名，而是遍历可见输入框并打分。

核心思路：

1. 取所有 `input`, `textarea`, `[contenteditable=true]`。
2. 过滤不可见、不可编辑、尺寸过小元素。
3. 读取 placeholder、aria-label、附近文案。
4. 查找最近容器内的按钮。
5. 根据语义和布局打分。
6. 返回最优候选。

这比在 Python 层硬堆 selector 更抗页面变动。

## 7.5 搜索按钮的降级策略

即使没找到明确按钮，也不应直接失败。

建议降级顺序：

1. 点击主按钮。
2. 在主输入框上按 `Enter`。
3. 若页面无变化，再尝试容器内第二高分按钮。

这样可以显著降低因为按钮类名变化导致的整体失败率。

## 7.6 建议引入“已验证控件缓存”

同一版本页面在一次会话中通常是稳定的。

因此建议：

1. 第一次成功定位主输入框和按钮后，缓存一份控件路径信息。
2. 后续搜索优先使用缓存结果。
3. 只有当缓存失效时，再重新跑探针。

缓存内容可以包括：

1. selector 或 DOM path
2. placeholder
3. 容器特征
4. 页面 URL 模式

这样能减少每次搜索都全量探测带来的波动。

## 7.7 实施建议

建议把当前输入框识别从：

1. 直接 `_first_visible_locator(...)`

升级为：

1. `_detect_search_controls(page)`
2. `_score_search_input_candidates(...)`
3. `_score_search_button_candidates(...)`
4. `_validate_search_controls(...)`

也就是说，把“找输入框”从一个工具函数升级为一个完整子模块逻辑。

## 8. 弱全自动阶段的目标工作流

建议工作流定义如下：

1. 用户输入 JD。
2. 系统完成岗位分析。
3. 系统生成结构化搜索意图和可执行搜索轮次。
4. 用户确认搜索轮次，或直接使用推荐方案。
5. 系统启动猎聘浏览器并检测登录。
6. 系统打开找人页并自动识别主输入框/按钮。
7. 系统按轮次搜索，例如：
   1. `结构 灯具`
   2. `结构 照明`
   3. `结构 LED`
   4. `结构 灯具 散热`
8. 每轮按页抓候选人概要。
9. 对候选人做去重。
10. 对通过摘要筛选的人抓详情。
11. 落 Excel。
12. 自动批量匹配。
13. 输出推荐结果。

补充执行约束：

1. 搜索计划执行过程中，不应一次性无等待地连续设置多个筛选项。
2. 每次设置筛选项后，都必须进入 `wait_for_filter_apply()` 之类的统一等待逻辑。
3. 等待逻辑至少需要观察：
   1. loading 是否出现
   2. loading 是否消失
   3. 结果区是否恢复稳定
   4. 当前字段值是否发生目标变化

## 9. 针对现有代码的改造建议

## 9.1 `prompt.py`

建议新增一段“搜索意图 JSON 输出要求”。

目标：让岗位分析除了 HTML 报告和 `MatchCriteria` 外，再输出一段供程序消费的结构化搜索意图。

## 9.2 `search_strategy_service.py`

当前职责偏向“从 HTML 提标签”。

建议升级职责为：

1. 解析搜索意图 JSON。
2. 把职位词拆解为原子词。
3. 生成可执行轮次。
4. 对输出做长度和角色过滤。

也就是说，这个模块要从“提词器”升级为“搜索计划生成器”。

## 9.3 `liepin_search_service.py`

建议新增：

1. 搜索主区域探针
2. 主输入框打分识别
3. 搜索按钮打分识别
4. 控件验证与缓存
5. 执行搜索轮次的方法，例如 `run_search_round(query)`

并补充分层实现：

1. 主搜索控件定位
2. 标签型筛选应用
3. 下拉型筛选应用
4. 弹窗确认型筛选应用
5. 区间输入型筛选应用
6. 页面刷新等待与结果稳定判断

## 9.4 `liepin_search_task_service.py`

建议改造为支持按多轮搜索计划执行，而不是默认从“当前结果页”直接入库。

新增能力：

1. 接收 `executable_rounds`
2. 按轮次逐个执行搜索
3. 每轮维护抓取上限
4. 跨轮次去重
5. 保存每个候选人的来源 query

## 9.5 数据记录

建议在现有任务记录中补充：

1. `executed_queries_json`
2. `search_control_snapshot_json`
3. `query_level_stats_json`

这样后续能分析：

1. 哪个 query 产出最多人
2. 哪个 query 噪音最大
3. 哪次页面控件识别失败

## 10. 建议的实施顺序

建议按以下顺序推进：

### 第一阶段：搜索词生成改造

1. 在 prompt 中新增结构化搜索意图输出。
2. 扩展 `SearchStrategyService`，输出 `atomic_terms` 和 `executable_rounds`。
3. 先用若干典型岗位验证搜索词质量。

验收标准：

1. 输出不再以完整职位名为主。
2. 首轮词以短词组合为主。
3. 每个岗位至少能自动产出 3-6 条可执行 query。

### 第二阶段：主搜索控件落地改造

先基于已验证的真实页面结构改造主搜索定位，不先追求泛化探针全覆盖。

实现：

1. 主按钮优先走 `button.search-btn`
2. 主搜索框优先从 `div.search-auto-complete-box` 中回找
3. 保留旧 selector 作为 fallback

验收标准：

1. 不依赖人工切到结果页，也能稳定在找人页执行一次主搜索
2. 不再误选职位名称/公司名称二级筛选框

### 第三阶段：筛选类型分流改造

实现：

1. 标签型字段：
   1. 工作年限
   2. 教育经历
   3. 院校要求
2. 下拉型字段：
   1. 性别
3. 弹窗确认型字段：
   1. 城市 `其他`
4. 页面刷新等待逻辑

验收标准：

1. 至少打通 `工作年限 + 性别 + 城市其他` 这三类不同交互模型
2. 每次筛选后都能等待页面稳定再继续

### 第四阶段：找人页探针与兜底增强

1. 实现搜索区域探针。
2. 实现输入框与按钮打分识别。
3. 实现一次真实交互验证。
4. 实现控件缓存和失效重探。

验收标准：

1. 在当前猎聘找人页上能稳定识别主输入框。
2. 能区分筛选输入框与主搜索输入框。
3. 页面小改动后，靠探针仍有恢复能力。

### 第五阶段：轮次搜索自动执行

1. 支持按 `executable_rounds` 自动搜索。
2. 支持跨 query 去重。
3. 支持分轮抓取统计。

验收标准：

1. 一个岗位可自动跑完多轮 query。
2. 同一候选人不会被重复落盘。
3. 每轮抓取数量和失败数可追踪。

## 11. 风险与边界

需要明确以下风险：

1. 猎聘页面结构仍可能变化，因此控件探针必须长期保留。
2. 搜索词再精细，也不可能完全替代顾问经验，因此必须保留人工编辑入口。
3. 不同行业的短词搜索规律差异很大，第一版规则应优先覆盖高频岗位类型。
4. 平台风控可能影响自动化稳定性，因此弱全自动必须允许人工接管和中途恢复。

## 12. 本阶段结论

弱全自动阶段最关键的不是“让 AI 多输出一些词”，而是把“岗位分析结果”变成“可执行搜索计划”。

这件事必须同时解决两个问题：

1. 搜索词层面：从完整职位名转向短词原子组合。
2. 页面执行层面：从脆弱 selector 转向搜索区域探针 + 控件打分识别。

只要这两件事做对，当前项目就能从“人手动切到结果页再抓取”升级到“系统自动搜索并抓取”的弱全自动阶段。

下一阶段开发建议优先顺序：

1. 先改 `SearchStrategyService` 和 `prompt.py`
2. 再改 `LiepinSearchService` 的控件识别机制
3. 最后改 `LiepinSearchTaskService` 的多轮搜索执行链路
