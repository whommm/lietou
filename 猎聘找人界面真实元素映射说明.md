# 猎聘找人界面真实元素映射说明

## 1. 文档目的

本文档基于当前线上猎聘 `找简历` 页面真实调试结果整理，不是基于代码猜测，也不是基于静态截图推断。

调试方式：

1. 通过 `OpenCode + @playwright/mcp` 连接真实页面。
2. 对当前页面进行可访问性快照、DOM 采样和结构验证。
3. 记录主搜索区、第一层筛选区、展开更多条件后的扩展筛选区的真实元素结构。

本文档的用途是：

1. 为后续弱全自动搜索改造提供真实页面依据。
2. 为 `liepin_search_service.py` 的定位逻辑改造提供元素映射参考。
3. 为后续实现“精准搜索条件自动设置”提供字段落点。

## 2. 当前页面信息

本次调试页面：

1. 初始 URL：`https://h.liepin.com/search/getConditionItem`
2. 触发筛选后的 URL 可能变为：`https://h.liepin.com/search/getConditionItem#session`
3. 页面标题：`找简历`

## 3. 总体结构

当前页面可分为四块：

1. 顶部主搜索区
2. 一级快捷筛选区
3. 二级扩展筛选区
4. 条件型弹窗区（如城市弹窗）

对应主要容器结构：

1. `div.search-top-bar`
2. `div.search-area`
3. `div.job-comp-condition`
4. `section.search-form-wrap`
5. `div.search-filter`
6. `div.filter-box`
7. `div.hide-search-cont`
8. `div.ant-modal.city-modal`

## 4. 顶部主搜索区映射

## 4.1 主搜索区容器

主搜索区关键容器如下：

1. `div.search-top-bar`
2. `div.search-area`
3. `div.search-auto-complete-box`

主搜索区文本特征：

1. `包含全部关键词`
2. `搜职位/公司/行业等（中文用空格隔开，英文用逗号隔开）`
3. `搜 索`

这三个文本一起出现时，可以较高置信度判定为主搜索区域。

## 4.2 关键词逻辑切换控件

左侧第一个控件不是搜索输入框，而是关键词逻辑切换控件。

真实结构：

1. 容器：`div.ant-select.ant-select-lg.h-select.switch-keyword-type.ant-select-single.ant-select-show-arrow`
2. 当前文本：`包含全部关键词`
3. 内部有一个不可编辑输入节点：`input.ant-select-selection-search-input`

用途判断：

1. 用于切换搜索关键词逻辑，例如“包含全部关键词”。
2. 不是主搜索输入框。

定位建议：

1. 不要把它当作关键词输入框。
2. 后续若需要切换搜索逻辑，应优先定位其外层 `switch-keyword-type` 容器，而不是内部 input。

## 4.3 主搜索输入框

当前页面顶部存在多个同类 `input.ant-select-selection-search-input`，真正的主搜索输入框特征如下：

1. 标签：`input`
2. class：`ant-select-selection-search-input`
3. role：`combobox`
4. 可编辑：`true`
5. 位置：`top=2`, `left=185`
6. 尺寸：`width=680`, `height=38`
7. 所在容器：
   1. `div.ant-select.ant-select-auto-complete.auto-input-wrap-v3.ant-select-single.ant-select-allow-clear.ant-select-show-search`
   2. 上层为 `div.search-auto-complete-box`

可见提示文案来自 placeholder 样式节点，不在 input 原生属性里：

1. `搜职位/公司/行业等（中文用空格隔开，英文用逗号隔开）`

实现结论：

1. 不要依赖 input 的 `placeholder` 属性，因为当前它为空。
2. 应通过父容器文本和尺寸判断它是主搜索框。

## 4.4 主搜索按钮

主搜索按钮映射明确：

1. 标签：`button`
2. class：`ant-btn ant-btn-primary search-btn`
3. 文本：`搜 索`
4. 位置：`top=1`, `left=874`
5. 尺寸：`width=98`, `height=42`

实现结论：

1. 当前页面下，`button.search-btn` 可以作为主按钮高优先级定位规则。
2. 后续如果 class 变化，再退化为“位于主搜索框右侧最近、文本为搜索的按钮”。

## 5. 一级快捷筛选区映射

一级快捷筛选区位于主搜索区下方，当前主要包括：

1. 职位名称
2. 公司名称
3. 正在发布的职位

## 5.1 职位名称

容器结构：

1. `div.search-item`
2. 标题：`span.search-item-title`，文本 `职位名称：`
3. 控件组合：
   1. 搜索输入：`div.ant-select.ant-select-lg.ant-select-auto-complete.auto-select-base.ant-select-single.ant-select-show-search`
   2. 当前文本：`搜索职位`
   3. 类型切换：`div.ant-select.ant-select-lg.h-select.switch-type.gray.ant-select-single.ant-select-show-arrow`
   4. 当前文本：`全部`

内部输入节点：

1. 可编辑输入框：`input.ant-select-selection-search-input`
2. 位置：`left=118`, `width=267`

结论：

1. 这是“职位名称二次筛选控件”，不是顶部主搜索框。

## 5.2 公司名称

容器结构：

1. `div.search-item`
2. 标题：`span.search-item-title`，文本 `公司名称：`
3. 控件组合：
   1. 搜索输入：`div.ant-select.ant-select-lg.ant-select-auto-complete.auto-select-base.ant-select-single.ant-select-show-search`
   2. 当前文本：`搜索公司`
   3. 类型切换：`div.ant-select.ant-select-lg.h-select.switch-type.gray.ant-select-single.ant-select-show-arrow`
   4. 当前文本：`全部`

内部输入节点：

1. 可编辑输入框：`input.ant-select-selection-search-input`
2. 位置：`left=608`, `width=267`

结论：

1. 这是“公司名称二次筛选控件”，不是顶部主搜索框。

## 5.3 一级快捷筛选与主搜索框的区别

顶部区域一共有三组相似控件：

1. 主关键词搜索框
2. 职位名称快捷筛选框
3. 公司名称快捷筛选框

区分规则：

1. 主搜索框宽约 `680`，位于 `left=185`
2. 职位筛选框宽约 `267`，位于 `left=118`
3. 公司筛选框宽约 `267`，位于 `left=608`
4. 主搜索框右侧存在 `button.search-btn`

实现建议：

1. 永远先定位主搜索按钮，再回找主搜索框。
2. 不要全页按“第一个可编辑 input”策略定位。

## 6. 第一层筛选区映射

该区域位于：

1. `section.search-form-wrap`
2. `div.search-filter`
3. `div.filter-box`

当前首层筛选字段如下。

## 6.1 目前城市

容器：

1. `div.search-item.sfilter-city`
2. 标题：`目前城市：`
3. 内容容器：`div.search-item-cont`

当前可见选项：

1. `不限`
2. `上海`
3. `广东`
4. `深圳`
5. `武汉`
6. `广州`
7. `河南`
8. `北京`
9. `杭州`
10. `其他`

子元素特征：

1. `span.unlimited-btn`
2. `div.tag-label-group`
3. 多个 `label.tag-item`
4. `span.btn-choose` 文本 `其他`

结论：

1. 这是标签式单选/多选筛选，不是下拉框。
2. `其他` 可能触发城市选择器弹层。

## 6.2 期望城市

容器：

1. `div.search-item.sfilter-city`
2. 标题：`期望城市：`

当前可见选项：

1. `不限`
2. `武汉`
3. `福建`
4. `广东`
5. `浙江`
6. `深圳`
7. `江苏`
8. `泰州`
9. `南京`
10. `其他`

结论：

1. 结构与 `目前城市` 相同，也是标签式选择。

## 6.3 工作年限

容器：

1. `div.search-item.sfilter-work-year`
2. 标题：`工作年限：`

当前可见选项：

1. `不限`
2. `应届生`
3. `1-3年`
4. `3-5年`
5. `5-10年`
6. `10年以上`
7. `自定义`

子结构：

1. `div.search-item-cont`
2. `div.choose-item`
3. `div.tag-label-group`
4. 多个 `label.tag-item`

当前选中项：

1. `label.tag-item.selected`，文本 `不限`

结论：

1. 这是标签式工作年限筛选。
2. `自定义` 很可能触发额外范围选择控件。

动态行为结论：

1. 点击标签后会立即切换选中态。
2. 例如点击 `1-3年` 后，`label.tag-item.selected` 会从 `不限` 切换到 `1-3年`。
3. 点击后不需要再手动点顶部搜索按钮。
4. 页面会自动触发刷新。
5. 刷新期间会出现：
   1. `ant-spin ant-spin-spinning`
   2. `resume-spin-box`
6. 触发筛选后 URL 可能追加 `#session`。

实现建议：

1. 这类字段应按“点击即生效型标签筛选”处理。
2. 点击后必须等待页面恢复稳定，不能连续立刻点下一个筛选项。

## 6.4 教育经历

容器：

1. `div.search-item.sfilter-edu`
2. 标题：`教育经历：`

当前可见学历选项：

1. `不限`
2. `本科`
3. `硕士`
4. `博士/博士后`
5. `大专`
6. `中专/中技`
7. `高中及以下`

额外子结构：

1. `div.edu-tz-wrap`
2. 标签：`统招要求：`
3. 下拉控件：`div.ant-select.ant-select-lg.edu-tz-select.ant-select-single.ant-select-show-arrow`
4. 当前值：`统招/非统招（不限）`

内部输入节点：

1. `input.ant-select-selection-search-input`
2. 不可编辑

结论：

1. 教育经历由“标签组 + 统招要求下拉”构成。
2. 学历本身不是下拉，是标签选择。
3. 统招要求才是下拉选择。

## 6.5 院校要求

容器：

1. `div.search-item.sfilter-additional`
2. 标题：`院校要求：`

当前可见选项：

1. `不限`
2. `211`
3. `985`
4. `双一流`
5. `海外留学`

子结构：

1. `div.tag-label-group`
2. 多个 `label.tag-item`

结论：

1. 这是标签式院校背景筛选。

## 6.6 年龄 / 活跃度 / 性别 / 跳槽频率

该区域出现在同一行：

1. 总容器：`div.sfilter-other-condition`
2. 当前整体文本：
   `年 龄： 活跃度： 不限 性 别： 不限 跳槽频率（不限）`

这说明：

1. `年龄` 字段当前页面上是存在的。
2. 但它没有像城市、年限那样直接以一组明显标签展示在外层。
3. 它与活跃度、性别、跳槽频率属于同一组复合筛选。

### 性别

已明确定位：

1. 容器：`div.search-item`
2. 标题：`性 别：`
3. 控件：`div.ant-select.ant-select-lg.h-select.sexSelectStyle.gray.ant-select-single.ant-select-show-arrow`
4. 当前值：`不限`

内部输入节点：

1. `input.ant-select-selection-search-input`
2. 不可编辑

结论：

1. 性别是一个下拉控件，不是标签组。
2. 性别与活跃度、跳槽频率属于同一组复合筛选行。

交互补充结论：

1. 直接点击性别控件外层，不一定总能稳定暴露出可见的选项层。
2. 更稳定的展开方式是：
   1. focus 到内部 `input.ant-select-selection-search-input`
   2. 使用键盘 `ArrowDown`
3. 聚焦后，内部 input 会出现：
   1. `aria-expanded="true"`
   2. `aria-controls="rc_select_9_list"`
   3. `aria-activedescendant="rc_select_9_list_0"`
4. 第二次 `ArrowDown` 后，`aria-activedescendant` 会变为 `rc_select_9_list_1`
5. 按 `Enter` 后即可选择对应选项。

选项层真实结构：

1. 下拉容器：`div.ant-select-dropdown.search-select.ant-select-dropdown-placement-bottomLeft`
2. 列表容器：`div.rc-virtual-list`
3. 单个选项：`div.ant-select-item.ant-select-item-option`
4. 当前已确认选项：
   1. `不限`
   2. `男`
   3. `女`

已完成的真实闭环验证：

1. focus 内部 input
2. 按 `ArrowDown`
3. 再按一次 `ArrowDown`
4. 按 `Enter`
5. 页面自动刷新

刷新行为：

1. 选择后不需要点击顶部搜索按钮。
2. 选择后会自动触发页面刷新。
3. 刷新期间会出现：
   1. `ant-spin ant-spin-spinning`
   2. `resume-spin-box`

实现建议：

1. 性别字段优先按“键盘驱动的下拉选择”实现。
2. 不要假设点击外层容器后总能直接拿到稳定可见的 portal 选项层。
3. 验证成功时，应优先观察：
   1. 字段当前显示值发生变化
   2. 页面进入并完成刷新

注意：

1. 本轮选择后字段文本出现过 `1 男` 这种混合显示，说明控件文本读取可能带入辅助状态字符。
2. 后续校验时不能只做严格等于 `男` 的文本判断，建议使用“包含 `男` 且不包含 `女`”之类的宽松判断。

### 活跃度

已明确定位：

1. 容器：`div.search-item`
2. 标题：`活跃度：`
3. 控件：`div.ant-select.ant-select-lg.h-select.sfilter-other-select.gray.ant-select-single.ant-select-show-arrow`
4. 当前值：`不限`

内部输入节点：

1. `input.ant-select-selection-search-input`
2. 不可编辑

结论：

1. 活跃度是一个下拉控件。
2. 从结构上看，它与性别使用同类 Ant Design Select 组件，后续大概率可复用相同展开/选择逻辑。

### 跳槽频率

已明确定位：

1. 容器：`div.search-item.line-wrap`
2. 当前值：`跳槽频率（不限）`
3. 控件：`div.ant-select.ant-select-lg.h-select.sfilter-other-select.gray.ant-select-single.ant-select-show-arrow`

内部输入节点：

1. `input.ant-select-selection-search-input`
2. 不可编辑

结论：

1. 跳槽频率是一个下拉控件。
2. 从结构上看，它与活跃度、性别使用同类 Ant Design Select 组件，后续大概率可复用相同展开/选择逻辑。

### 年龄

本轮调试已确认年龄是区间输入型字段，不是下拉。

真实结构：

1. 所在复合区域：`div.sfilter-other-condition`
2. 两个输入框：
   1. `input.ant-input.ant-input-lg.age-input`，placeholder 为 `岁`
   2. `input.ant-input.ant-input-lg.age-input`，placeholder 为 `不限`

结论：

1. 年龄字段是输入区间型字段。
2. 它与活跃度、性别、跳槽频率并列显示在同一行，但交互类型不同。

实现建议：

1. 年龄字段应单独按“区间输入型筛选”处理。
2. 不能把它和性别/活跃度按同一种下拉逻辑实现。

## 7. 二级扩展筛选区映射

点击 `展开更多条件` 后，会出现扩展筛选区域：

1. 容器：`div.hide-search-cont`

当前可见文本：

1. `语 言： 不限普通话英语日语法语粤语其他`
2. `期望年薪：`
3. `目前年薪：`
4. `期望行业： 不限`
5. `期望职位： 不限`
6. `毕业院校： 请输入学校名称`
7. `专业名称： 请输入专业名称`
8. `其 他： 求职状态（不限） 简历语言（不限） 海外工作经验 管理经验`

## 7.1 语言

容器：

1. `div.search-item.sfilter-lang`
2. 标题：`语 言：`

当前可见选项：

1. `不限`
2. `普通话`
3. `英语`
4. `日语`
5. `法语`
6. `粤语`
7. `其他`

结构判断：

1. 与城市、年限类似，属于标签式选择。

## 7.2 求职状态

位于 `其 他：` 区域内。

已定位到：

1. 容器：`div.ant-select.ant-select-lg.h-select.sfilter-other-select.apply-job-status.ant-select-multiple.ant-select-show-arrow`
2. 当前值：`求职状态（不限）`

结论：

1. 求职状态是多选下拉控件。

## 7.2.1 简历语言

位于 `其 他：` 区域内。

已定位到：

1. 容器：`div.ant-select.ant-select-lg.h-select.sfilter-other-select.gray.ant-select-single.ant-select-show-arrow`
2. 当前值：`简历语言（不限）`

结论：

1. 简历语言是单选下拉控件。

## 7.2.2 毕业院校

已定位到：

1. 容器：`div.search-item.sfilter-school-box`
2. 外层输入控件：`div.ant-select.ant-select-lg.ant-select-auto-complete.ant-select-single.ant-select-show-search`
3. 内部可编辑输入节点：`input.ant-select-selection-search-input`
4. 可见提示文本：`请输入学校名称`

结论：

1. 毕业院校是输入补全型字段。

## 7.2.3 专业名称

已定位到：

1. 容器：`div.search-item.sfilter-speciality-box`
2. 外层输入控件：`div.ant-select.ant-select-lg.ant-select-auto-complete.ant-select-single.ant-select-show-search`
3. 内部可编辑输入节点：`input.ant-select-selection-search-input`
4. 可见提示文本：`请输入专业名称`

结论：

1. 专业名称是输入补全型字段。

## 7.3 简历语言 / 海外工作经验 / 管理经验

当前确认：

1. 它们位于扩展区 `其 他：` 行内
2. 文本已确认存在
3. 本次未逐个拆出最终控件类型

当前结论：

1. 它们属于可进一步深挖的扩展筛选字段
2. 在当前弱全自动第一阶段中，优先级低于城市、年限、教育、院校、性别

## 8. 当前可直接落地的自动化字段

基于本次调试，后续代码中最适合优先实现的精准筛选字段如下：

1. 主关键词搜索框
2. 搜索按钮
3. 目前城市
4. 期望城市
5. 工作年限
6. 教育经历
7. 统招要求
8. 院校要求
9. 性别
10. 语言
11. 活跃度
12. 跳槽频率
13. 年龄
14. 毕业院校
15. 专业名称

这些字段都已经拿到了明确文本和较稳定容器特征。

但需要区分交互复杂度：

1. 结构已明确，不等于第一批就应全部实现。
2. 其中有些字段虽然已定位，但交互型式更复杂，仍建议晚于基础标签筛选接入。

## 9. 当前不建议第一批直接实现的字段

以下字段虽然存在，但建议暂不作为第一批自动化落地点：

1. 期望年薪
2. 目前年薪
3. 期望行业
4. 期望职位
5. 简历语言
6. 海外工作经验
7. 管理经验

原因：

1. 虽然部分字段已定位，但尚未完成完整交互闭环验证
2. 部分字段涉及输入补全、多选、弹层或确认流程
3. 当前弱全自动阶段优先级没那么高

## 10.1 城市 `其他` 弹窗映射

本轮调试已确认：

1. 点击 `目前城市` 或 `期望城市` 行中的 `其他`，会弹出城市选择弹窗。
2. 这不是“点击即生效型”筛选，而是“弹窗确认型”筛选。

### 弹窗外层结构

1. 遮罩：`div.ant-modal-mask`
2. 包裹层：`div.ant-modal-wrap.antd-fd-city-modal.ant-modal-centered`
3. 主弹窗：`div.ant-modal.city-modal`
4. 角色：`role="dialog"`

### 标题与关闭

1. 标题容器：`div.ant-modal-title`
2. 标题文本：`请选择城市`
3. 标题文本节点：`span.city-header-text`
4. 关闭按钮：`span.city-modal-close`

### 城市搜索输入框

已明确定位：

1. 元素：`input.ant-input`
2. placeholder：`搜索城市`
3. 位置大致：`top=83`, `left=404`, `width=249`

结论：

1. 当目标城市不在可见热门城市中时，应优先走这个输入框完成搜索。

### 城市搜索联想列表

本轮进一步确认：

1. 在 `搜索城市` 输入框中输入城市名后，会出现联想结果列表。
2. 例如输入 `长沙` 后，弹窗标题区域和联想层中会出现：`湖南 · 长沙`。

联想结果结构：

1. 外层：`div.suggest-list`
2. 列表：`ul`
3. 单项：`li`
4. 文本容器：`p`
5. 城市名节点：内部 `span`，文本如 `长沙`

当前已确认的联想示例：

1. `湖南 · 长沙`

交互结论：

1. 当城市不在热门城市可见标签中时，可以通过 `搜索城市` 输入框完成检索。
2. 后续应优先点击联想结果项，而不是依赖左侧省份菜单逐层展开。

### 左侧区域分类

左侧结构：

1. 外层：`div.antd-lp-city-sider.antd-lp-city-sider-tab`
2. 国内/海外切换：`div.antd-lp-city-sider-switch`
3. 菜单：`ul.ant-city-menu-list`

当前已确认菜单项：

1. `历史/热门`
2. `北京`
3. `上海`
4. `天津`
5. `重庆`
6. `港澳台`
7. `河北`
8. `山西`
9. `辽宁`
10. `吉林`
11. `黑龙江`
12. `江苏`
13. `浙江`
14. `安徽`
15. `福建`
16. `江西`
17. `山东`
18. `河南`
19. `湖北`
20. `湖南`
21. `广东`
22. `广西`
23. `海南`
24. `四川`
25. `贵州`
26. `云南`
27. `陕西`
28. `甘肃`
29. `青海`
30. `西藏`
31. `宁夏`
32. `新疆`
33. `内蒙古`

### 右侧热门城市列表

右侧结构：

1. 外层：`div.data-container`
2. 标题：`span.step-text.last-step`，文本 `热门城市`
3. 城市列表：`div.data-list`
4. 单个城市项：`span.ant-tag.ant-tag-checkable`

当前已确认热门城市：

1. `全国`
2. `北京`
3. `上海`
4. `广州`
5. `深圳`
6. `天津`
7. `苏州`
8. `重庆`
9. `南京`
10. `杭州`
11. `大连`
12. `成都`
13. `武汉`
14. `西安`

### 已选区与确认按钮

1. 已选区：`div.antd-lp-city-data-result`
2. 文本示例：`已选（0/9）`
3. 确认按钮容器：`div.result-btn`
4. 真正确认按钮：`button.ant-btn.ant-btn-primary`
5. 按钮文本：`确认`

### 城市 `其他` 的交互结论

1. 点击 `其他` 后不会立刻应用筛选。
2. 必须先在弹窗内完成城市选择。
3. 然后点击 `确认`。
4. 之后页面才会应用筛选并触发刷新。
5. 城市选择有两条路径：
   1. 直接点击右侧热门城市标签
   2. 通过顶部 `搜索城市` 输入框检索后点击联想结果

### 城市弹窗完整闭环验证结果

本轮已完成一次真实闭环验证：

1. 在 `搜索城市` 输入框中输入 `长沙`
2. 出现联想结果 `湖南 · 长沙`
3. 点击联想结果后，底部已选区由 `已选（0/9）` 变为 `已选（1/9）`
4. 点击 `确认`
5. 弹窗关闭
6. 页面自动触发刷新
7. 刷新期间出现：
   1. `ant-spin ant-spin-spinning`
   2. `resume-spin-box`
8. 刷新完成后，`目前城市` 行文本变为：
   `目前城市： 不限 长沙 上海 广东 深圳 武汉 广州 河南 北京 杭州 其他`

这说明：

1. 通过搜索联想选中的城市，会被插入到当前城市标签列表前部。
2. 点击 `确认` 后会自动应用筛选并刷新结果页。

### 城市筛选实现风险点

本轮还确认了两个实现风险：

1. `目前城市` 应用 `长沙` 后，页面上仍可能同时看到 `不限`。
2. 此时仅通过是否还存在 `不限`，不能判断筛选是否失败。

因此后续验证规则不能只看：

1. `不限` 是否消失

而应优先看：

1. 目标城市是否出现在当前城市行中
2. 结果页是否进入并完成刷新
3. 候选人结果是否出现对应城市倾向

### 城市筛选的推荐验证标准

建议按以下顺序判断城市筛选是否成功：

1. 城市弹窗已关闭
2. 结果区进入 loading 并恢复稳定
3. `目前城市` 或 `期望城市` 行中出现目标城市文本
4. 若目标城市来自搜索联想，则应能在行内看到该城市被插入到现有标签序列中

因此：

1. 城市 `其他` 必须按“弹窗确认型字段”处理。
2. 不能把它当作普通标签点击型字段实现。

## 10. 实施建议

建议后续代码改造按两批推进。

### 第一批

先做：

1. 主搜索框
2. 搜索按钮
3. 城市类标签筛选
4. 工作年限标签筛选
5. 教育经历标签筛选
6. 院校要求标签筛选
7. 性别下拉筛选
8. 城市 `其他` 弹窗链路

### 第二批

再做：

1. 年龄
2. 活跃度
3. 跳槽频率
4. 年薪
5. 行业/职位期望
6. 学校/专业输入型筛选

说明：

1. 虽然性别已经完成下拉闭环验证，但活跃度和跳槽频率尚未逐项实测。
2. 它们大概率可以复用性别的同类实现，但在正式落地前仍建议各自做一次真实选择验证。

## 11. 关键实现原则

基于本次真实页面调试，后续实现要遵守以下原则：

1. 不要只靠全局 `input` selector 识别控件。
2. 优先通过“标题文本 + 父容器 class + 子结构类型”三元组定位。
3. 标签式筛选与下拉式筛选必须区分处理。
4. 对于 Ant Design 组件，不要把内部 `input.ant-select-selection-search-input` 误当成真正业务字段本体。
5. 对于“其他”“自定义”“展开更多条件”这类入口，应单独视为会触发二级交互的控件。
6. 至少要区分四种字段类型：
   1. 点击即生效型标签筛选
   2. 下拉型筛选
   3. 弹窗确认型筛选
   4. 区间输入型筛选
7. 对于点击即生效型标签筛选，点击后必须等待页面完成自动刷新。
8. 对于弹窗确认型筛选，必须完成“打开弹窗 -> 选择 -> 确认 -> 等待刷新”的完整链路。

## 12. 本次调试结论

本次真实页面调试已经证明：

1. 猎聘找人页不是简单的“几个输入框 + 一个搜索按钮”。
2. 它本质上是：
   1. 顶部主搜索
   2. 一级快捷筛选
   3. 二级扩展筛选
   4. 条件型弹窗
   5. 多种控件类型混合
3. 后续自动化实现必须按“字段映射”思路推进，而不是按“再补几个 selector”推进。
4. 至少 `工作年限` 已确认属于“点击即生效并自动刷新”的字段。
5. 至少 `城市 -> 其他` 已确认属于“打开弹窗并点击确认后才生效”的字段。

当前已经足以支持第一批精准搜索改造。
