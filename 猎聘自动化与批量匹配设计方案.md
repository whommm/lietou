# 猎聘自动化与批量匹配设计方案

## 1. 文档目的

本文档用于定义当前项目下一阶段的完整建设方案：在现有 `公司调研`、`岗位分析`、`简历匹配` 三个闭环基础上，新增 `猎聘自动搜索`、`候选人简历采集入库`、`批量匹配评分` 三项能力，形成完整的候选人获取与筛选工作流。

本文档不是临时讨论稿，而是后续开发、调整、排错和验收的参考基线。除非后续明确决策变更，否则新增功能均按本文档的模块边界、数据结构和实施顺序推进。

## 2. 当前项目基础

当前项目已具备以下能力：

1. `公司调研`：输入公司名称，调用 Tavily + LLM 输出结构化调研报告。
2. `岗位分析`：输入 JD，输出结构化岗位分析结果与搜索建议。
3. `简历匹配`：输入岗位与单份简历，输出推荐结论、风险点和推进建议。
4. `本地配置管理`：通过 `config.json` 存储接口配置。
5. `历史记录`：通过本地 JSON 保存岗位分析、公司调研、简历匹配结果。
6. `桌面 GUI`：基于 `CustomTkinter` 构建统一工作台。
7. `HTML 渲染输出`：通过 `tkinterweb` 渲染 LLM 输出的结构化 HTML。

这些基础能力已经证明：项目具备 AI 分析能力、桌面工作流承载能力和本地持久化基础。下一阶段不需要推翻现有系统，而是在既有能力上补齐“候选人自动获取与批量筛选”链路。

## 3. 新阶段目标

### 3.1 总体目标

从当前三闭环升级为六能力闭环：

1. 公司调研
2. 岗位分析
3. 猎聘自动搜索
4. 候选人详情采集
5. 候选人入库管理
6. 批量匹配评分与排序

### 3.2 目标工作流

完整工作流定义如下：

1. 用户输入 JD 并完成岗位分析。
2. 系统基于岗位分析生成结构化搜索策略。
3. 用户确认搜索任务参数。
4. Playwright 启动固定浏览器实例。
5. 用户在该浏览器中手动登录猎聘。
6. 工具接管浏览器，自动执行搜索。
7. 工具遍历搜索结果页并进入候选人详情页。
8. 工具提取完整简历文本并入本地候选人库。
9. 用户发起批量匹配任务。
10. 系统基于目标岗位对候选人进行批量评分与排序。
11. 用户查看候选人列表、单人详细报告、风险点与推进建议。

## 4. 范围定义

### 4.1 本阶段纳入范围

1. 使用 `Playwright` 实现浏览器自动化。
2. 采用“工具启动浏览器，用户手动登录，工具复用登录态”的方案。
3. 支持在猎聘执行搜索。
4. 支持提取候选人完整简历文本。
5. 支持候选人结构化入库。
6. 支持对候选人进行批量匹配评分。
7. 支持批量结果排序、查看、回放。
8. 支持搜索任务、采集任务、评分任务的本地持久化。

### 4.2 本阶段明确不做

1. 不自动执行猎聘登录流程。
2. 不接管用户已经打开的任意外部浏览器窗口。
3. 不同时支持多个招聘平台。
4. 不把现有全部 JSON 历史记录统一迁移到数据库。
5. 不做云端多端同步。

## 5. 设计原则

本阶段开发遵循以下原则：

1. 不做临时脚本式实现，按长期维护标准设计。
2. 不把浏览器自动化逻辑直接写进 UI 文件。
3. 采集、解析、持久化、评分、展示五层解耦。
4. 新增数据能力从第一天起使用数据库，不继续堆大型 JSON。
5. 所有长任务必须可跟踪状态、可失败定位、可中断恢复。
6. 尽量沿用当前项目风格和目录结构，减少不必要重构。
7. 每一步实现都按最终架构推进，而不是为后续重写埋临时代码。

## 6. 总体架构

### 6.1 逻辑分层

新增能力按如下五层组织：

1. `UI 层`
2. `任务编排层`
3. `站点自动化层`
4. `数据存储层`
5. `匹配分析层`

### 6.2 分层职责

#### UI 层

负责：

1. 展示搜索任务配置。
2. 展示候选人库。
3. 展示批量匹配结果。
4. 提供启动、暂停、刷新、查看详情等交互。

不负责：

1. 直接处理浏览器页面定位。
2. 直接拼接数据库 SQL。
3. 直接执行简历解析。

#### 任务编排层

负责：

1. 搜索任务创建与执行。
2. 采集任务调度。
3. 批量匹配任务调度。
4. 状态变更与进度汇总。

#### 站点自动化层

负责：

1. 启动浏览器。
2. 登录态检测。
3. 搜索页操作。
4. 结果列表采集。
5. 候选人详情页提取。

#### 数据存储层

负责：

1. 候选人库。
2. 搜索任务记录。
3. 批量评分结果。
4. 候选人与来源关系映射。

#### 匹配分析层

负责：

1. 候选人文本标准化。
2. 批量匹配评分。
3. 单人详细报告生成。
4. 批量结果概览摘要。

## 7. 目录与模块规划

建议新增并调整为以下结构：

```text
src/
  core/
    config.py
    history.py
    llm_client.py
    company_research_client.py
    prompt.py

    liepin_browser.py
    liepin_search_service.py
    liepin_resume_extractor.py
    search_strategy_service.py
    candidate_repository.py
    search_task_repository.py
    batch_match_service.py
    batch_match_repository.py
    database.py

  models/
    candidate.py
    search_task.py
    batch_match.py

  ui/
    main_window.py
    company_research_widget.py
    job_analysis_widget.py
    resume_match_widget.py
    candidate_library_widget.py
    batch_match_widget.py
    history_widget.py
    history_picker.py
    html_renderer.py

  utils/
    helpers.py
    html_sanitizer.py
    text_normalizer.py
    retry.py
    time_utils.py
```

## 8. 新增模块职责定义

### 8.1 `src/core/database.py`

职责：

1. 管理 SQLite 连接。
2. 初始化表结构。
3. 提供基础事务和连接获取能力。

要求：

1. 数据库文件存放在项目根目录。
2. 初始化必须幂等。
3. 不与 UI 代码耦合。

### 8.2 `src/core/liepin_browser.py`

职责：

1. 使用 Playwright 启动持久化 Chromium 浏览器。
2. 管理固定 `user_data_dir`。
3. 检测猎聘登录状态。
4. 提供页面打开和关闭能力。

关键要求：

1. 采用同步 API `playwright.sync_api`。
2. 统一管理浏览器生命周期。
3. 不在外部模块重复持有 Playwright 连接。

### 8.3 `src/core/liepin_search_service.py`

职责：

1. 打开猎聘搜索页。
2. 输入关键词并触发搜索。
3. 解析搜索结果列表。
4. 翻页并提取候选人详情链接。
5. 把候选人基础信息转换为统一结构。

### 8.4 `src/core/liepin_resume_extractor.py`

职责：

1. 打开候选人详情页。
2. 提取结构化简历模块。
3. 清洗页面噪音信息。
4. 生成标准化 `resume_text`。
5. 返回结构化候选人对象。

### 8.5 `src/core/search_strategy_service.py`

职责：

1. 根据岗位 JD 和岗位分析结果生成结构化搜索策略。
2. 将 AI 输出转为可执行搜索词集合。
3. 区分精准词、扩池词、布尔式预留字段。

该模块的引入目的是避免 RPA 直接依赖最终展示 HTML。

### 8.6 `src/core/search_task_repository.py`

职责：

1. 创建搜索任务。
2. 更新任务状态。
3. 保存执行参数与错误信息。
4. 查询历史搜索任务。

### 8.7 `src/core/candidate_repository.py`

职责：

1. 保存候选人信息。
2. 候选人去重。
3. 记录候选人与搜索任务的来源关系。
4. 支持分页查询、条件过滤、按评分排序。

### 8.8 `src/core/batch_match_service.py`

职责：

1. 基于岗位和候选人列表发起批量匹配。
2. 调用现有 LLM 匹配能力。
3. 生成概览评分结果和单人详细报告。
4. 维护评分状态。

### 8.9 `src/core/batch_match_repository.py`

职责：

1. 保存批量匹配任务。
2. 保存每位候选人的评分结果。
3. 支持结果排序和历史回看。

## 9. 数据模型设计

### 9.1 搜索任务模型

定义：

```text
SearchTask
- id
- job_history_id
- task_name
- keywords_json
- search_mode
- max_pages
- max_candidates
- status
- current_step
- error_message
- created_at
- started_at
- finished_at
```

字段说明：

1. `job_history_id`：关联岗位分析历史记录。
2. `keywords_json`：存储搜索词数组及其分类。
3. `search_mode`：预留 `keyword`、`boolean`。
4. `status`：取值建议为 `pending`、`running`、`completed`、`failed`、`cancelled`。
5. `current_step`：例如“打开搜索页”“解析第 2 页”“提取候选人详情”。

### 9.2 候选人模型

定义：

```text
Candidate
- id
- platform
- platform_candidate_id
- profile_url
- name
- current_title
- current_company
- city
- work_years
- education
- resume_text
- resume_summary
- raw_payload_json
- created_at
- updated_at
```

说明：

1. `platform` 当前固定为 `liepin`。
2. `platform_candidate_id` 若页面无法直接获取，可暂时为空。
3. `profile_url` 是第一优先去重依据。
4. `raw_payload_json` 用于保留原始提取片段，便于排错。

### 9.3 候选人来源关系模型

定义：

```text
CandidateSource
- id
- candidate_id
- search_task_id
- keyword
- page_number
- rank_index
- fetched_at
```

作用：

1. 记录候选人来自哪个搜索任务。
2. 记录由哪个关键词命中。
3. 支持同一候选人被多个搜索词命中的分析。

### 9.4 批量匹配任务模型

定义：

```text
BatchMatchJob
- id
- job_history_id
- search_task_id
- candidate_count
- status
- created_at
- started_at
- finished_at
- error_message
```

### 9.5 批量匹配结果模型

定义：

```text
BatchMatchResult
- id
- batch_job_id
- candidate_id
- score
- recommendation
- summary
- risks
- full_report_html
- status
- created_at
- updated_at
```

推荐枚举：

1. `strong_recommend`
2. `recommend_after_screen`
3. `need_more_info`
4. `not_recommended`

## 10. 数据库存储方案

### 10.1 存储策略

采用双轨制：

1. 现有功能历史记录继续使用 JSON，不主动迁移。
2. 新增候选人库、搜索任务、批量匹配任务使用 SQLite。

这样可以在不破坏现有稳定功能的前提下，为新功能提供合适的数据结构。

### 10.2 数据库文件

建议数据库文件名：

```text
liepin_workbench.db
```

位置：项目根目录。

### 10.3 建议表结构

#### `search_tasks`

```sql
id TEXT PRIMARY KEY,
job_history_id TEXT NOT NULL,
task_name TEXT NOT NULL,
keywords_json TEXT NOT NULL,
search_mode TEXT NOT NULL,
max_pages INTEGER NOT NULL,
max_candidates INTEGER NOT NULL,
status TEXT NOT NULL,
current_step TEXT,
error_message TEXT,
created_at TEXT NOT NULL,
started_at TEXT,
finished_at TEXT
```

#### `candidates`

```sql
id TEXT PRIMARY KEY,
platform TEXT NOT NULL,
platform_candidate_id TEXT,
profile_url TEXT NOT NULL,
name TEXT,
current_title TEXT,
current_company TEXT,
city TEXT,
work_years TEXT,
education TEXT,
resume_text TEXT,
resume_summary TEXT,
raw_payload_json TEXT,
created_at TEXT NOT NULL,
updated_at TEXT NOT NULL
```

索引建议：

1. `UNIQUE(profile_url)`
2. `INDEX(current_company)`
3. `INDEX(name)`

#### `candidate_sources`

```sql
id TEXT PRIMARY KEY,
candidate_id TEXT NOT NULL,
search_task_id TEXT NOT NULL,
keyword TEXT NOT NULL,
page_number INTEGER,
rank_index INTEGER,
fetched_at TEXT NOT NULL
```

#### `batch_match_jobs`

```sql
id TEXT PRIMARY KEY,
job_history_id TEXT NOT NULL,
search_task_id TEXT,
candidate_count INTEGER NOT NULL,
status TEXT NOT NULL,
created_at TEXT NOT NULL,
started_at TEXT,
finished_at TEXT,
error_message TEXT
```

#### `batch_match_results`

```sql
id TEXT PRIMARY KEY,
batch_job_id TEXT NOT NULL,
candidate_id TEXT NOT NULL,
score INTEGER,
recommendation TEXT,
summary TEXT,
risks TEXT,
full_report_html TEXT,
status TEXT NOT NULL,
created_at TEXT NOT NULL,
updated_at TEXT NOT NULL
```

## 11. 浏览器接管方案

### 11.1 登录模式

明确采用以下方案：

1. 工具启动 Playwright 持久化浏览器。
2. 用户手动完成猎聘登录。
3. 工具复用登录态继续执行。
4. 后续任务默认使用同一用户目录。

### 11.2 方案原因

此方案相比“自动登录”或“接管外部浏览器窗口”更适合当前项目：

1. 避免登录流程自动化复杂度。
2. 避免验证码、短信验证、滑块等问题。
3. 避免对用户现有浏览器环境造成干扰。
4. 工具对 Playwright 自己启动的浏览器控制更稳定。

### 11.3 浏览器用户目录

建议固定目录：

```text
browser_profile/liepin
```

存放内容：

1. Cookie
2. localStorage
3. sessionStorage
4. 浏览器登录态缓存

### 11.4 登录态检测

登录态检测规则需要基于真实页面确认，原则如下：

1. 优先检查“我的账号”“退出”“简历搜索页可访问”等已登录页面特征。
2. 若跳转到登录页或检测到登录按钮，判定未登录。
3. 未登录时，工具提示用户手动完成登录，不自动继续执行搜索任务。

## 12. 搜索策略设计

### 12.1 为什么要单独做搜索策略层

当前岗位分析结果是 HTML 展示内容，不适合作为 RPA 直接输入源。为了后续维护稳定，应新增结构化搜索策略对象。

### 12.2 搜索策略对象

定义：

```text
SearchStrategy
- precise_keywords
- expansion_keywords
- synonyms
- exclude_keywords
- boolean_queries
- source_company_hints
```

### 12.3 第一阶段实际使用策略

为降低实现复杂度并提升稳定性，第一阶段真正用于猎聘搜索的主策略为：

1. `precise_keywords`
2. `expansion_keywords`

`boolean_queries` 在数据结构中保留，但不作为首批强依赖路径。

### 12.4 搜索任务创建方式

岗位分析完成后，系统提供：

1. 自动生成搜索词。
2. 人工勾选或确认搜索词。
3. 设置抓取页数、抓取人数上限。
4. 创建搜索任务。

## 13. 猎聘搜索执行流程

### 13.1 搜索流程

定义标准流程：

1. 打开猎聘搜索页。
2. 检查是否已登录。
3. 填写搜索框。
4. 触发搜索。
5. 等待结果页加载完成。
6. 提取当前页候选人卡片信息。
7. 进入候选人详情页。
8. 提取完整简历。
9. 返回列表或继续下一个候选人。
10. 翻页继续，直到达到上限。

### 13.2 翻页规则

必须支持：

1. 配置最大页数。
2. 配置最大候选人数。
3. 如果达到人数上限，则提前停止。
4. 翻页失败时允许记录错误并中止任务。

### 13.3 页面定位原则

必须遵循：

1. 尽量使用稳定 DOM 定位，不使用屏幕坐标。
2. 统一封装选择器，不散落在业务逻辑中。
3. 尽量使用等待可见、等待可点击，而非纯 `sleep`。
4. 仅在必要时使用短延迟模拟人工节奏。

## 14. 完整简历文本提取策略

### 14.1 提取目标

目标不是简单复制页面所有文本，而是生成适合匹配分析的高质量标准化简历文本。

### 14.2 提取层次

采用三层提取策略：

#### 第一层：结构化区域提取

优先提取：

1. 基本信息
2. 当前职位与公司
3. 工作经历
4. 项目经历
5. 教育经历
6. 自我评价
7. 技能标签

#### 第二层：文本标准化

执行：

1. 去除导航、按钮、无关控件文字。
2. 合并多余空行。
3. 统一字段标题。
4. 保留段落结构。
5. 生成标准化 `resume_text`。

#### 第三层：全文兜底提取

若部分区域无法稳定识别，则提取主内容区域全文，作为降级补充。

### 14.3 标准化简历文本格式

建议内部统一为：

```text
【候选人基础信息】
姓名：
当前职位：
当前公司：
工作年限：
所在城市：
学历：

【个人概述】
...

【工作经历】
1. ...
2. ...

【项目经历】
...

【教育经历】
...

【补充信息】
...
```

### 14.4 入库前摘要

建议额外生成 `resume_summary`，用于候选人列表快速预览。长度建议控制在 `150-300` 字以内。

## 15. 候选人去重与更新策略

### 15.1 去重依据

去重优先级：

1. `profile_url`
2. `platform_candidate_id`
3. 备用：姓名 + 当前公司 + 当前职位组合

### 15.2 重复候选人处理规则

若候选人已存在：

1. 更新 `updated_at`。
2. 保留较完整的 `resume_text`。
3. 新增一条 `candidate_sources` 记录，表明其被新的关键词或任务再次命中。

### 15.3 原始数据保留

即使候选人已存在，也应保留本次抓取的原始来源关系，方便后续追踪哪个搜索词最有效。

## 16. 批量匹配评分设计

### 16.1 输入来源

批量匹配任务输入包括：

1. 目标岗位分析记录。
2. 一批候选人 `resume_text`。

### 16.2 输出设计

输出分为两层：

#### 概览层

用于列表展示：

1. 综合匹配分数
2. 推荐动作
3. 一句话摘要
4. 关键风险摘要

#### 详情层

用于单人查看：

1. 完整 HTML 匹配报告
2. 完整推进建议
3. 详细验证问题

### 16.3 评分状态

每位候选人评分应独立维护状态：

1. `pending`
2. `running`
3. `completed`
4. `failed`

这样单个候选人失败时不会阻塞整批任务。

### 16.4 推荐结果标准化

建议统一映射到固定枚举，便于 UI 筛选：

1. `strong_recommend`
2. `recommend_after_screen`
3. `need_more_info`
4. `not_recommended`

## 17. UI 设计方案

### 17.1 页签规划

主窗口最终建议包含 5 个标签页：

1. `公司调研`
2. `岗位分析`
3. `简历匹配`
4. `候选人库`
5. `批量匹配`

### 17.2 岗位分析页改动

新增能力：

1. 显示结构化搜索策略摘要。
2. 增加“创建猎聘搜索任务”入口。
3. 支持把当前岗位分析记录发送到候选人库页。

### 17.3 候选人库页功能

主要包含三块：

#### 顶部任务区

1. 选择岗位分析记录。
2. 展示搜索策略。
3. 选择搜索词。
4. 设置最大页数。
5. 设置最大候选人数。
6. 启动浏览器。
7. 检查登录状态。
8. 开始搜索与采集。

#### 中间候选人列表区

展示字段建议包括：

1. 姓名
2. 当前职位
3. 当前公司
4. 城市
5. 学历
6. 来源关键词
7. 最近更新时间
8. 是否已评分

#### 右侧详情区

用于展示：

1. 原始标准化简历文本
2. 候选人来源信息
3. 最近一次评分结果摘要

### 17.4 批量匹配页功能

主要包含：

1. 选择岗位。
2. 选择候选人集合。
3. 发起批量评分任务。
4. 查看评分进度。
5. 查看排序列表。
6. 点开查看单人完整报告。

## 18. 任务状态与日志设计

### 18.1 搜索任务状态

建议状态机：

1. `pending`
2. `running`
3. `completed`
4. `failed`
5. `cancelled`

### 18.2 批量评分任务状态

建议状态机：

1. `pending`
2. `running`
3. `completed`
4. `failed`
5. `cancelled`

### 18.3 运行日志

建议新增结构化日志文件，记录：

1. 任务 ID
2. 当前步骤
3. 搜索词
4. 页码
5. 候选人链接
6. 错误原因
7. 时间戳

日志用途：

1. 页面结构变化时定位问题。
2. 候选人详情提取失败时定位问题。
3. 批量评分异常时快速回溯。

## 19. 异常处理设计

### 19.1 错误分类

建议明确区分以下错误：

1. 浏览器启动失败
2. Playwright 依赖或浏览器缺失
3. 猎聘未登录
4. 搜索页结构变化
5. 结果列表为空
6. 候选人详情页提取失败
7. 数据库存储失败
8. LLM 匹配评分失败

### 19.2 处理策略

1. 单候选人失败：记录错误并继续处理下一个候选人。
2. 单页部分失败：尽量跳过失败项并继续。
3. 搜索页整体失效：任务失败并提示修复。
4. 浏览器未登录：暂停等待用户处理。
5. 数据库存储失败：立即中止并提示。

## 20. 性能与节奏控制

### 20.1 页面操作节奏

虽然使用 DOM 级自动化，不采用纯坐标点击，但仍建议保持接近人工节奏：

1. 页面跳转后等待稳定。
2. 翻页间加入短延迟。
3. 详情页采集完成后再回列表。
4. 不在极短时间内高频点击。

### 20.2 批量评分节奏

建议第一版先采用：

1. 顺序评分
2. 或极低并发评分

原因：

1. 当前项目是桌面端工具，稳定性优先。
2. 批量评分主要瓶颈在 LLM 请求。
3. 顺序评分更容易定位失败项。

## 21. 测试策略

### 21.1 单元测试

重点覆盖：

1. 文本标准化逻辑
2. 去重逻辑
3. 数据库增删改查
4. 搜索策略对象转换
5. 评分结果映射逻辑

### 21.2 集成测试

重点覆盖：

1. Playwright 浏览器启动
2. 用户目录复用
3. 登录态检测
4. 候选人详情页解析器

### 21.3 人工回归清单

上线前必须人工验证：

1. 打开浏览器是否正常
2. 登录后会话是否复用
3. 搜索页是否可正常执行
4. 候选人详情是否可正常提取
5. 简历文本是否完整
6. 批量评分结果是否可正确展示

## 22. 依赖与环境要求

### 22.1 当前环境结论

当前项目使用虚拟环境 `Python 3.8.10`。该版本可承载本阶段 Playwright 方案，不作为当前阻塞问题。

### 22.2 新增依赖

预计新增：

1. `playwright`
2. 可能需要的页面解析辅助库，视实现决定

要求：

1. 所有新增依赖必须锁定版本。
2. 需补充项目依赖文件。
3. 需增加 Playwright 浏览器安装说明。

### 22.3 浏览器安装

除 Python 包外，还需执行：

```bash
playwright install chromium
```

## 23. 分阶段实施计划

本项目按最终架构分阶段落地，不采用临时拼接方案。

### 阶段 1：架构与数据层定稿

目标：

1. 建立数据库。
2. 定义数据模型。
3. 明确模块接口。

产出：

1. `database.py`
2. 数据表初始化逻辑
3. `models/` 数据类
4. 各 repository 空壳和接口

### 阶段 2：Playwright 浏览器会话层

目标：

1. 启动持久化浏览器。
2. 检测登录态。
3. 打开猎聘搜索页。

产出：

1. `liepin_browser.py`
2. 浏览器配置项
3. 登录态检查逻辑

### 阶段 3：搜索任务与搜索页自动化

目标：

1. 创建搜索任务。
2. 执行关键词搜索。
3. 提取列表页候选人。

产出：

1. `search_task_repository.py`
2. `liepin_search_service.py`
3. 搜索任务状态更新逻辑

### 阶段 4：候选人详情页采集与入库

目标：

1. 提取完整简历。
2. 生成标准化文本。
3. 入候选人库并去重。

产出：

1. `liepin_resume_extractor.py`
2. `text_normalizer.py`
3. `candidate_repository.py`

### 阶段 5：批量匹配服务

目标：

1. 批量选择候选人。
2. 逐个生成评分结果。
3. 保存概览结果和详细报告。

产出：

1. `batch_match_service.py`
2. `batch_match_repository.py`
3. 批量结果模型与状态流转

### 阶段 6：UI 整合

目标：

1. 新增候选人库页。
2. 新增批量匹配页。
3. 打通岗位分析到搜索任务的联动。

产出：

1. `candidate_library_widget.py`
2. `batch_match_widget.py`
3. `main_window.py` 页签扩展与事件整合

### 阶段 7：鲁棒性与回归

目标：

1. 增强错误处理。
2. 增强日志。
3. 增强人工回归清单。
4. 完成文档补充。

## 24. 当前决策结论

截至本文档版本，以下决策已经确认：

1. 使用 `Playwright`，不走传统屏幕坐标式 RPA。
2. 目标是提取 `完整简历文本`，而非仅列表摘要。
3. 登录方式采用 `用户手动登录，工具接管并复用登录态`。
4. 当前 `Python 3.8.10` 可继续使用，不作为本阶段阻塞。
5. 新功能按长期维护架构设计，不采用临时型 MVP 拼接方案。

## 25. 下一步建议

后续开发按以下顺序展开：

1. 先实现 `数据库与数据模型`。
2. 再实现 `Playwright 浏览器会话层`。
3. 再实现 `搜索任务 + 搜索执行`。
4. 再实现 `候选人详情提取与入库`。
5. 再实现 `批量匹配服务`。
6. 最后做 `UI 整合与回归`。

当前推荐立即进入 `阶段 1：架构与数据层定稿` 的代码实施。
