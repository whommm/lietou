# 继续开发交接说明 - 猎聘找人自动化

## 当前目标

当前阶段不再继续做页面探索，下一窗口应直接进入代码实现。

实现重点：

1. 改造 `src/core/liepin_search_service.py`
2. 让猎聘找人页的主搜索框、搜索按钮、关键筛选字段具备稳定定位能力
3. 优先支持弱全自动所需的最小可用链路

## 必读文件

新窗口开始后，优先阅读以下文件：

1. `猎聘找人界面真实元素映射说明.md`
2. `弱全自动化改造方案.md`
3. `src/core/liepin_search_service.py`
4. `src/core/search_strategy_service.py`
5. `src/core/liepin_search_task_service.py`

其中最重要的是：

`猎聘找人界面真实元素映射说明.md`

## 已确认的页面真实结构

### 1. 主搜索区

主搜索按钮：

1. `button.search-btn`
2. 文本：`搜 索`

主搜索输入框：

1. `input.ant-select-selection-search-input`
2. 顶部主搜索框是宽度约 `680` 的那个
3. 所在容器：`div.search-auto-complete-box`
4. 可见提示文本来自外层，不在 input placeholder 上：
   `搜职位/公司/行业等（中文用空格隔开，英文用逗号隔开）`

关键词逻辑切换控件：

1. 左侧 `包含全部关键词`
2. 不是主搜索框

### 2. 一级快捷筛选

已确认：

1. `职位名称`
2. `公司名称`

它们不是主搜索框，不要误用。

### 3. 第一层筛选字段

已完成结构定位：

1. `目前城市`
2. `期望城市`
3. `工作年限`
4. `教育经历`
5. `统招要求`
6. `院校要求`
7. `年龄`
8. `性别`

### 4. 二级扩展筛选字段

已完成结构定位：

1. `语言`
2. `毕业院校`
3. `专业名称`
4. `求职状态`

## 已确认的三种关键交互模型

### A. 点击即生效型

样板字段：`工作年限`

已验证行为：

1. 点击标签后立即切换选中态
2. 不需要点顶部搜索按钮
3. 页面自动刷新
4. 会出现 `ant-spin ant-spin-spinning` 和 `resume-spin-box`
5. URL 可能保持 `#session`

### B. 弹窗确认型

样板字段：`目前城市 -> 其他`

已验证完整闭环：

1. 点击 `其他`
2. 打开城市弹窗：`div.ant-modal.city-modal`
3. 可用顶部搜索框：
   `input.ant-input[placeholder="搜索城市"]`
4. 输入 `长沙` 后出现联想列表：
   `div.suggest-list > ul > li`
   示例：`湖南 · 长沙`
5. 点击联想项后，底部已选从 `已选（0/9）` 变为 `已选（1/9）`
6. 点击 `确认` 按钮：
   `button.ant-btn.ant-btn-primary`
7. 弹窗关闭
8. 页面自动刷新

重要结论：

1. 城市选择有两条路径：
   1. 右侧热门城市标签
   2. 顶部 `搜索城市` 输入框 + 联想项
2. 这是“弹窗确认型”字段，不是普通标签点击型字段
3. 应用城市后，不要仅靠 `不限` 是否消失判断成功
4. 应优先判断：
   1. 弹窗关闭
   2. 页面刷新完成
   3. 目标城市出现在当前城市行中

### C. 键盘驱动下拉型

样板字段：`性别`

已验证完整闭环：

1. 不要优先点击外层壳体
2. 优先 focus 内部 input：
   `input.ant-select-selection-search-input`
3. 按 `ArrowDown`
4. 再按一次 `ArrowDown`
5. 按 `Enter`
6. 页面自动刷新

已确认下拉层结构：

1. `div.ant-select-dropdown.search-select.ant-select-dropdown-placement-bottomLeft`
2. `div.rc-virtual-list`
3. `div.ant-select-item.ant-select-item-option`
4. 当前已验证选项：
   1. `不限`
   2. `男`
   3. `女`

注意：

1. 选中后字段文本可能出现 `1 男` 之类混合值
2. 后续校验不能严格等于 `男`
3. 建议做宽松匹配，例如“包含 `男` 且不包含 `女`”

## 当前实现优先级建议

下一窗口应优先实现这些能力：

1. 主搜索框定位
2. 搜索按钮定位
3. 城市 `其他` 弹窗链路
4. 工作年限标签点击链路
5. 教育经历 / 院校要求标签链路
6. 性别下拉链路

不建议当前窗口优先实现：

1. 活跃度
2. 跳槽频率
3. 年薪
4. 行业/职位期望
5. 毕业院校/专业名称的完整补全闭环

原因：

1. 当前弱全自动第一版未必需要
2. 已经有足够字段支撑精准搜索第一版

## 下一窗口建议的具体动作

1. 先读 `猎聘找人界面真实元素映射说明.md`
2. 然后直接改 `src/core/liepin_search_service.py`

建议方向：

1. 新增“主搜索控件定位”逻辑
2. 新增“字段类型分派”逻辑
3. 新增至少三类交互方法：
   1. 标签型
   2. 下拉型
   3. 弹窗确认型
4. 新增统一等待页面稳定的方法

## 建议给下一窗口的第一句话

你可以在新窗口直接这样说：

`先阅读 继续开发交接说明-猎聘找人自动化.md 和 猎聘找人界面真实元素映射说明.md，然后直接开始实现 liepin_search_service 的主搜索控件定位和城市/工作年限/性别筛选链路。`
