# 猎聘找人页探针实现设计

## 1. 结论

对当前项目而言，**内建“页面探针 + 自动取证 + 交互验证”机制**，是现阶段最简单且最可靠的实现方式。

这里的“最简单”是指：

1. 不需要引入新的运行基础设施。
2. 不依赖外部 MCP 是否可用。
3. 不需要改变现有 Playwright 架构。
4. 可以直接复用当前 `LiepinBrowserManager`、`LiepinSearchService` 和 `debug_artifacts` 目录体系。

这里的“最可靠”是指：

1. 页面变化后仍能留下结构化证据。
2. 失败时能知道是“没找到主输入框”还是“找到了但点错了”。
3. 不是只依赖某一条脆弱 selector。
4. 可以通过真实交互验证，闭环判断主搜索控件是否正确。

因此，本设计文档默认采用此方案，不再优先考虑外部 MCP、浏览器插件或人工临时抓 selector 的方向。

## 2. 目标

本设计的目标不是“单次把 selector 调出来”，而是让系统具备以下长期能力：

1. 自动识别猎聘找人页顶部主搜索区域。
2. 自动识别最可能的主搜索输入框。
3. 自动识别与该输入框对应的搜索按钮。
4. 自动进行一次轻量交互验证。
5. 自动导出可供程序和人分析的调试证据。
6. 在控件识别失败时，为后续修复提供可复现依据。

## 3. 范围

本设计仅覆盖“猎聘找人页主搜索控件识别与调试”问题。

本阶段纳入范围：

1. 页面探针。
2. 候选输入框采样与评分。
3. 候选按钮采样与评分。
4. 输入框与按钮配对。
5. 轻量交互验证。
6. 调试报告导出。
7. 成功结果缓存。

本阶段不做：

1. 自动登录。
2. 候选人结果卡片抓取逻辑重写。
3. 猎聘全站通用控件识别引擎。
4. 多平台兼容抽象。

## 4. 为什么这是最优先方案

## 4.1 为什么不优先继续补 selector

继续补 selector 的问题：

1. 页面顶部存在多个输入框和按钮，命中不等于正确。
2. selector 很容易随着类名或层级变化而失效。
3. 失败时通常不知道是“没命中”还是“命中了错误输入框”。
4. 调试时只能反复手工试错。

也就是说，补 selector 只能解决一时问题，不能提升系统自解释能力。

## 4.2 为什么不优先依赖外部 MCP

外部 MCP 或浏览器调试接入的问题：

1. 你当前项目本身没有这类接入基础设施。
2. 即使接上，仍要自己定义如何识别主搜索控件。
3. 这类工具适合辅助排查，不适合作为系统长期运行依赖。
4. 对最终产品能力提升有限，更多是开发便利性增强。

因此，对当前项目而言，MCP 不是“解决问题的核心”，最多只是“额外调试便利工具”。

## 4.3 为什么页面探针更合适

页面探针方案的优势：

1. 识别逻辑在项目内部可控。
2. 失败时能自动留证。
3. 未来 agent 可直接读结构化调试文件分析问题。
4. 成功时还能缓存结果，减少重复探测。

## 5. 总体设计

建议新增一套“找人页探针”子系统，分为五层：

1. 页面取证层
2. 控件采样层
3. 控件评分层
4. 交互验证层
5. 调试导出层

整体流程如下：

1. 打开猎聘找人页。
2. 采集页面顶部可见输入框和按钮信息。
3. 识别若干候选搜索区域。
4. 对候选输入框和按钮进行评分。
5. 选出最优输入框与最优按钮。
6. 使用测试 query 进行一次轻量验证。
7. 验证成功则缓存；失败则导出报告。

## 6. 模块设计

## 6.1 新增数据结构

建议新增以下 dataclass，位置建议放在：

`src/core/liepin_search_probe.py`

### `ElementRect`

```text
ElementRect
- x: float
- y: float
- width: float
- height: float
```

### `ProbeInputCandidate`

```text
ProbeInputCandidate
- index: int
- tag_name: str
- input_type: str
- placeholder: str
- aria_label: str
- title: str
- visible: bool
- editable: bool
- value_preview: str
- rect: ElementRect
- container_text: str
- nearby_buttons: list[str]
- score: int
- score_reasons: list[str]
- dom_path: str
```

### `ProbeButtonCandidate`

```text
ProbeButtonCandidate
- index: int
- tag_name: str
- text: str
- visible: bool
- enabled: bool
- rect: ElementRect
- container_text: str
- score: int
- score_reasons: list[str]
- dom_path: str
```

### `SearchControlMatch`

```text
SearchControlMatch
- input_candidate_index: int
- button_candidate_index: int
- combined_score: int
- reasons: list[str]
```

### `SearchControlValidationResult`

```text
SearchControlValidationResult
- typed_query: str
- clicked: bool
- enter_pressed: bool
- url_changed: bool
- result_count_changed: bool
- page_marker_changed: bool
- success: bool
- error_message: str
```

### `SearchProbeReport`

```text
SearchProbeReport
- url: str
- title: str
- generated_at: str
- inputs: list[ProbeInputCandidate]
- buttons: list[ProbeButtonCandidate]
- best_match: SearchControlMatch | None
- validation: SearchControlValidationResult | None
- screenshot_path: str
- html_path: str
- json_path: str
```

## 6.2 新增服务模块

建议新增文件：

`src/core/liepin_search_probe.py`

职责：

1. 采样页面输入框和按钮。
2. 对候选控件打分。
3. 生成最佳控件配对。
4. 进行轻量交互验证。
5. 生成结构化 probe report。

建议主类：

`LiepinSearchProbeService`

## 7. 采样设计

## 7.1 采样目标

页面探针首先不是找“唯一正确元素”，而是完整采集“所有像搜索控件的候选元素”。

采样对象：

1. `input`
2. `textarea`
3. `[contenteditable="true"]`
4. `button`
5. 可点击的 `a`, `div`, `span`

采样范围：

1. 整页可见元素
2. 重点关注页面顶部 35% 高度区域

这样做的原因是：猎聘主搜索区通常在顶部，但为了兼容页面小改动，不应写死绝对层级。

## 7.2 页面内执行方式

建议使用 `page.evaluate(...)` 在浏览器端执行探针脚本，原因：

1. 浏览器端更容易拿到真实布局信息。
2. 批量采样性能更好。
3. 容易取最近容器文本、相邻按钮、DOM path。

采样字段建议包括：

1. 标签名
2. input type
3. placeholder
4. aria-label
5. title
6. 当前 value 预览
7. 是否可见
8. 是否可编辑
9. bounding rect
10. 所在容器文本
11. 最近按钮文本列表
12. DOM path

## 7.3 DOM path 设计

`dom_path` 不要求是绝对 XPath，只要能较稳定重定位即可。

建议优先生成：

1. `id`
2. `data-*` 属性路径
3. 结合标签名和同级序号的简化 CSS path

示例：

```text
div.search-wrap > div.search-box > input:nth-of-type(1)
```

它主要用于：

1. 调试显示
2. 成功识别后的控件缓存

## 8. 评分设计

## 8.1 输入框评分规则

输入框评分建议满分 100，按以下维度累计。

### 正向加分

1. placeholder 包含 `搜索`：+25
2. placeholder 包含 `关键词`：+20
3. placeholder 包含 `职位` / `人选`：+12
4. aria-label 或 title 有同类语义：+8
5. 可见且可编辑：+10
6. 宽度 >= 260：+8
7. 位于页面顶部 35% 区域：+6
8. 同容器存在 `搜索` 按钮：+20
9. 同容器文本含 `找人` / `搜索`：+8

### 负向扣分

1. 宽度过小：-10
2. 不可编辑：-20
3. 处于筛选区且附近存在 `不限`、`下拉选择` 等文本：-15
4. placeholder 更像公司筛选、城市筛选而非关键词输入：-18
5. 明显是隐藏输入框：直接剔除

### 输入框候选过滤阈值

建议：

1. `score < 20` 直接舍弃
2. 只保留前 5 个输入框候选

## 8.2 按钮评分规则

按钮评分建议满分 100。

### 正向加分

1. 文本是 `搜索`：+35
2. 文本包含 `找人`：+20
3. 与高分输入框同容器：+20
4. 位于输入框右侧且距离近：+15
5. 元素可点击：+10

### 负向扣分

1. 文本是 `重置`：-30
2. 文本是筛选类按钮：-20
3. 距离输入框太远：-15
4. 不可点击：-20

### 按钮候选过滤阈值

建议：

1. `score < 20` 舍弃
2. 只保留前 5 个按钮候选

## 8.3 配对规则

输入框和按钮不是各自独立选最高分，而是要做配对评分。

配对维度：

1. 是否同容器
2. 是否位于同一水平行附近
3. 按钮是否在输入框右侧
4. 输入框和按钮单独得分
5. 容器语义是否像搜索区

最终生成一个 `best_match`。

## 9. 交互验证设计

## 9.1 为什么必须做验证

仅靠布局和文本判断，仍可能选中错误的输入框。

因此必须做一次轻量验证，才能把“像搜索控件”升级为“确认是搜索控件”。

## 9.2 验证方式

建议测试 query 使用一个轻量且低风险的值，例如：

`结构 灯具`

验证顺序：

1. 记录验证前 URL。
2. 记录验证前结果页卡片数量或页面标记。
3. 向候选输入框写入测试 query。
4. 优先点击候选按钮。
5. 若点击失败，则回退为按 `Enter`。
6. 等待短时间页面变化。
7. 检查：
   1. URL 是否变化
   2. 结果卡片数量是否变化
   3. 是否出现结果页特征元素
8. 若满足条件，则验证成功。

## 9.3 验证成功判定

建议至少满足以下任一组条件：

1. URL 变化 + 结果卡片可见
2. URL 不变，但结果卡片数量变化明显
3. 出现新的搜索结果区域标记

## 9.4 验证失败处理

验证失败时：

1. 不要立即抛异常结束。
2. 记录失败原因。
3. 尝试使用第二高分配对再验证一次。
4. 若仍失败，再导出调试报告并抛出诊断性异常。

## 10. 调试导出设计

## 10.1 输出目录

建议沿用现有目录：

`debug_artifacts/liepin/`

新增子目录规则：

`search_probe_YYYYMMDD_HHMMSS/`

## 10.2 输出文件

每次探针执行建议导出：

1. `page.html`
2. `page.png`
3. `controls.json`
4. `search_probe.json`
5. `summary.txt`

## 10.3 `controls.json`

保存原始采样结果，包括：

1. 所有输入框候选
2. 所有按钮候选
3. rect
4. container_text
5. dom_path

## 10.4 `search_probe.json`

保存最终诊断结果，包括：

1. best_match
2. score reasons
3. validation result
4. error_message

## 10.5 `summary.txt`

便于快速肉眼查看的概要：

1. 当前 URL
2. 最佳输入框摘要
3. 最佳按钮摘要
4. 验证是否成功
5. 若失败，失败原因

## 11. 缓存设计

## 11.1 为什么需要缓存

同一会话内，猎聘找人页结构通常是稳定的。

因此没有必要每次搜索都重新完整探测。

## 11.2 缓存内容

建议缓存：

1. 页面 URL 模式
2. 输入框 `dom_path`
3. 按钮 `dom_path`
4. 上次验证成功时间
5. 上次验证 query

## 11.3 缓存位置

可选两种方式：

1. 内存缓存：用于单次应用运行期
2. 本地 JSON 缓存：用于跨次启动复用

弱全自动第一阶段建议先做内存缓存即可。

## 11.4 缓存失效条件

以下情况触发重新探测：

1. 缓存 path 不再可用
2. 缓存控件不可见
3. 缓存控件验证失败
4. 页面 URL 模式变化明显

## 12. 与现有代码的集成方式

## 12.1 `LiepinSearchService`

建议新增方法：

1. `probe_search_controls(page, test_query: str = "结构 灯具")`
2. `get_verified_search_controls(page)`
3. `_run_search_probe(page, test_query)`
4. `_try_cached_search_controls(page)`

建议改造现有方法：

1. `_find_candidate_search_inputs(...)`
2. `_find_primary_search_input(...)`
3. `_submit_search(...)`

改造后逻辑：

1. 优先尝试缓存控件
2. 缓存失效则运行探针
3. 探针成功则返回验证通过的输入框与按钮
4. 探针失败则抛出带调试路径的异常

## 12.2 `LiepinBrowserManager`

建议复用现有：

1. `get_debug_dir()`
2. `export_debug_snapshot(...)`

可新增一个辅助方法：

1. `export_page_artifacts(page, reason: str)`

职责：

1. 输出 html
2. 输出 screenshot
3. 输出 console 或基础页面信息

## 12.3 UI 接入

建议在候选人抓取页新增一个按钮：

`诊断找人页`

点击后执行：

1. 启动浏览器
2. 打开找人页
3. 执行探针
4. 弹窗显示结果和文件路径

这样可以让你在不跑完整抓取任务的情况下单独调试搜索控件识别。

## 13. 异常与输出约定

建议新增异常类型：

`LiepinSearchControlProbeError`

错误信息格式建议为：

```text
未能识别猎聘主搜索控件。
调试目录：E:\Myproject\lietou\debug_artifacts\liepin\search_probe_20260426_150500
失败原因：最高分输入框验证未通过，第二候选也验证失败。
```

这样主流程拿到错误后，用户和开发者都知道去哪里看证据。

## 14. 实施顺序

建议按以下顺序实施：

### 第一步：只做采样与报告

先实现：

1. 采样所有输入框和按钮
2. 输出 `controls.json`
3. 输出截图和 HTML

验收标准：

1. 能稳定看到页面顶部有哪些候选控件
2. 不需要肉眼翻整页 HTML 才知道问题

### 第二步：加入评分机制

实现：

1. 输入框打分
2. 按钮打分
3. 配对评分
4. 输出 `search_probe.json`

验收标准：

1. 能稳定选出 1-2 组高可信候选控件

### 第三步：加入交互验证

实现：

1. 测试 query 写入
2. 按钮点击或回车验证
3. 验证结果记录

验收标准：

1. 探针能给出“识别成功/失败”的最终结论

### 第四步：接入主搜索链路

实现：

1. 搜索主流程优先使用验证通过的控件
2. 缓存成功控件
3. 失败时自动重探

验收标准：

1. 搜索执行不再主要依赖固定 selector

## 15. 验收标准

本设计完成后，应满足以下标准：

1. 页面顶部存在多个输入框时，系统能给出主搜索控件候选排序。
2. 失败时一定能导出结构化调试文件，而不是只报“未找到输入框”。
3. 页面轻微改版后，仍有较高概率通过探针恢复识别。
4. 主流程可以使用验证成功的控件缓存，不需要每次全量探测。
5. 用户可以通过“诊断找人页”入口单独调试，而不必跑完整抓取任务。

## 16. 后续演进空间

完成本方案后，后续还可以再加：

1. 可访问性树辅助评分
2. 控件识别结果持久化缓存
3. 自动比较多次 probe 结果，检测页面结构漂移
4. 结合结果页采集成功率反向修正搜索控件评分权重

但这些都不是第一阶段必须项。

## 17. 最终结论

对当前项目来说，最好的路线不是继续堆 selector，也不是优先引入外部 MCP，而是：

1. 先把“找人页主搜索控件识别”做成可诊断、可验证、可留证的内部能力。
2. 再把这个能力接入现有搜索流程。

这条路线最务实，也最贴近你现在代码库的真实状态。
