# 候选人 Excel 工作流改造计划

## 1. 背景与目标

当前项目已经具备岗位分析、搜索策略生成、猎聘候选人抓取、批量匹配分析等能力，但候选人管理部分逐渐承担了过多的数据展示和状态管理职责，带来了以下问题：

1. GUI 承担过多数据管理责任，复杂度持续上升。
2. 候选人库页面越来越像半套数据库前端，不符合当前使用场景。
3. 候选人查看、筛选、对比、人工补充判断，其实更适合放在表格中完成。
4. 当前候选人抓取和批量匹配结果不够适合直接交付、归档和人工协作。

本次改造的目标是将候选人工作流调整为：

1. 程序不再使用内部数据库作为候选人主存储。
2. 候选人抓取结果直接落到 Excel 文件中。
3. 批量匹配从 Excel 导入候选人数据，并将结果回写到同一个 Excel。
4. 匹配结果统一改为纯文本，移除 HTML 报告渲染链路。
5. UI 从“候选人管理界面”收缩为“任务控制台”。

最终形成的工作方式是：

1. 岗位分析 -> 生成搜索策略。
2. 候选人抓取 -> 创建岗位 Excel 文件并边抓边写。
3. Excel 成为候选人数据唯一载体。
4. 批量匹配 -> 手动导入 Excel -> 匹配所有有简历详情的行 -> 回写 Excel。

## 2. 核心设计原则

### 2.1 Excel 作为唯一候选人存储载体

候选人信息不再写入本地 SQLite 数据库，也不再依赖程序内部候选人表来做长期管理。

程序仅负责：

1. 抓取候选人数据。
2. 将候选人数据写入 Excel。
3. 从 Excel 读取候选人数据。
4. 执行批量匹配分析。
5. 将匹配结果写回 Excel。

### 2.2 边抓边写

抓取过程采用“先落索引，再补详情”的模式：

1. 先从搜索结果页抓取候选人概要信息。
2. 立即写入 Excel 一行，形成索引记录。
3. 再进入候选人详情页抓取完整简历。
4. 抓取完成后回写同一行。
5. 每处理一个候选人就保存一次，保证中断后 Excel 仍保留进度。

### 2.3 批量匹配只处理有数据的行

批量匹配时，程序从 Excel 中读取所有满足以下条件的候选人：

1. `简历详情` 非空。
2. `简历抓取状态` 为 `抓取成功` 或 `部分成功`。

其余行全部跳过。

### 2.4 匹配结果统一为纯文本

批量匹配结果不再保留 HTML 报告，不再依赖 HTML 渲染器，不再生成 HTML 展示内容。

保留的匹配结果字段为：

1. `匹配度分数`
2. `匹配详情`

其中：

1. `匹配度分数` 为 0-100 整数。
2. `匹配详情` 为纯文本长内容，可包含多段文字。

## 3. Excel 文件方案

### 3.1 文件命名规则

每次抓取某个岗位时，创建一个新的 Excel 文件。

文件名格式建议为：

`岗位名称_YYYYMMDD_HHMMSS.xlsx`

例如：

`后端工程师_20260406_184500.xlsx`

### 3.2 文件存放位置

建议在工作区下统一创建导出目录：

`exports/candidates/`

文件路径示例：

`exports/candidates/后端工程师_20260406_184500.xlsx`

### 3.3 工作表结构

第一阶段采用单工作表，避免引入不必要复杂度。

工作表名称建议为：

`候选人`

### 3.4 Excel 列定义

最终列结构采用以下版本：

1. `序号`
2. `姓名`
3. `年龄`
4. `当前岗位`
5. `当前公司`
6. `城市`
7. `工作年限`
8. `学历`
9. `来源关键词`
10. `页码`
11. `排名`
12. `简历链接`
13. `简历抓取状态`
14. `简历详情`
15. `匹配度分数`
16. `匹配详情`
17. `抓取时间`
18. `匹配时间`

### 3.5 字段说明

#### `序号`

按结果页抓取顺序递增，用于阅读和人工核对。

#### `姓名`

候选人姓名。

#### `年龄`

如果页面能稳定提取则写入，否则允许为空，不作为程序流程依赖字段。

#### `当前岗位`

候选人当前职位名称。

#### `当前公司`

候选人当前公司。

#### `城市`

候选人所在城市。

#### `工作年限`

候选人工作年限。

#### `学历`

候选人学历。

#### `来源关键词`

当前批次岗位对应的搜索关键词标签。

#### `页码`

候选人在结果页中的页码位置。

#### `排名`

候选人在该页中的顺序位置。

#### `简历链接`

候选人详情页链接。

#### `简历抓取状态`

状态值固定为以下几种之一：

1. `待抓取`
2. `抓取成功`
3. `抓取失败`
4. `部分成功`

#### `简历详情`

候选人详情页抓取后的纯文本简历内容。

#### `匹配度分数`

批量匹配结果分数，范围 0-100。

#### `匹配详情`

匹配模型输出的纯文本分析内容。

#### `抓取时间`

该候选人写入 Excel 的时间。

#### `匹配时间`

该候选人最近一次被批量匹配并回写结果的时间。

## 4. 抓取流程设计

### 4.1 抓取流程总览

抓取流程改为：

1. 用户在岗位分析中生成岗位搜索策略。
2. 用户进入候选人抓取页面。
3. 用户选择岗位并配置抓取页数、抓取人数。
4. 程序创建 Excel 文件。
5. 程序读取当前猎聘结果页。
6. 每发现一位候选人，就先写索引行。
7. 再逐个抓详情页，写入 `简历详情` 和 `简历抓取状态`。
8. 全部处理完成后，提示文件路径并允许用户直接打开文件。

### 4.2 抓取时的 Excel 写入规则

对于每个候选人，程序应按以下顺序执行：

1. 从结果页提取基础字段。
2. 生成 Excel 新行。
3. 填充基础字段：姓名、岗位、公司、城市、工作年限、学历、关键词、页码、排名、链接、抓取时间。
4. 将 `简历抓取状态` 初始化为 `待抓取`。
5. 保存 Excel。
6. 打开详情页抓取完整简历。
7. 成功则写入 `简历详情`，状态更新为 `抓取成功`。
8. 若抓到部分内容，则状态更新为 `部分成功`。
9. 若失败，则状态更新为 `抓取失败`，`简历详情` 可保留空值或错误摘要。
10. 再次保存 Excel。

### 4.3 抓取中断处理

因为 Excel 是唯一候选人载体，必须保证程序中断后文件仍然可用。

建议实现：

1. 每处理完一个候选人就保存一次。
2. 当前 Excel 文件在抓取过程中始终可被恢复读取。
3. 若程序异常退出，用户仍可通过现有文件查看已抓取进度。

### 4.4 失败行保留策略

对于抓取失败的候选人，不跳过、不删除，保留该行。

保留原因：

1. 方便用户看到本次抓取总量。
2. 方便后续人工判断。
3. 方便未来支持失败重试。

## 5. 批量匹配流程设计

### 5.1 批量匹配入口调整

批量匹配不再依赖候选人数据库，不再依赖当前候选人库页面状态。

新的批量匹配入口改为：

1. 用户选择岗位。
2. 用户手动选择一个 Excel 文件。
3. 程序读取该 Excel 中的候选人数据。
4. 程序筛选所有可匹配行。
5. 执行批量匹配。
6. 将结果回写到原 Excel。

### 5.2 可匹配行定义

程序读取 Excel 后，只对满足以下条件的行执行匹配：

1. `简历详情` 非空。
2. `简历抓取状态` 为 `抓取成功` 或 `部分成功`。

### 5.3 回写规则

对每一行候选人，完成匹配后写入：

1. `匹配度分数`
2. `匹配详情`
3. `匹配时间`

如果 Excel 中原来已经存在匹配结果，则新结果直接覆盖旧结果。

### 5.4 匹配结果文本格式

匹配结果不再保留 HTML，也不再要求结构化 HTML 输出。

建议模型输出格式为纯文本，例如：

1. 第一行给出总体结论。
2. 第二部分描述匹配亮点。
3. 第三部分描述明显短板。
4. 第四部分给出建议动作。

程序只负责提取分数和保存全文，不再渲染富文本。

## 6. 系统改造范围

### 6.1 需要新增的模块

#### Excel 读写服务

新增统一的 Excel 服务层，例如：

`src/core/candidate_excel_service.py`

该模块负责：

1. 创建工作簿。
2. 创建表头。
3. 追加候选人索引行。
4. 更新候选人详情行。
5. 读取 Excel 中的候选人数据。
6. 回写批量匹配结果。
7. 保存文件。

#### Excel 文件选择与打开能力

UI 层需要支持：

1. 创建 Excel 后显示文件路径。
2. 提供“打开当前 Excel”按钮。
3. 提供“导入 Excel 文件”按钮。

### 6.2 需要重构的模块

#### 候选人抓取流程

需要重构当前依赖 `CandidateRepository` 的流程，改为直接调用 Excel 服务。

涉及模块：

1. `src/core/liepin_search_task_service.py`
2. `src/ui/main_window.py`
3. `src/ui/candidate_library_widget.py`

#### 批量匹配流程

需要将当前依赖候选人数据库的逻辑改为 Excel 导入模式。

涉及模块：

1. `src/core/batch_match_service.py`
2. `src/ui/main_window.py`
3. `src/ui/batch_match_widget.py`

### 6.3 需要删除或弱化的模块

#### 候选人数据库相关模块

以下模块将不再作为候选人主链路依赖：

1. `src/core/candidate_repository.py`
2. `src/models/candidate.py`
3. 与候选人表、候选人来源表相关的数据库初始化逻辑

这些模块可分阶段处理：

1. 先停止新链路调用。
2. 再逐步移除或降级为兼容代码。

#### 候选人管理型 UI

当前候选人库 UI 中的大量列表管理、详情管理、状态管理逻辑将失去必要性。

需要改为：

1. 抓取任务配置
2. 抓取进度展示
3. Excel 文件操作入口

#### HTML 匹配展示链路

以下能力应删除或大幅简化：

1. HTML 渲染器
2. HTML 报告回写
3. HTML 解析与 fallback 逻辑
4. 基于 HTML 的详细结果展示区

## 7. UI 调整方案

### 7.1 候选人页改名建议

当前 `候选人库` 页面建议改为：

`候选人抓取`

该页面职责只保留：

1. 选择岗位
2. 查看搜索策略
3. 配置抓取页数和数量
4. 启动浏览器
5. 检查登录
6. 开始抓取并生成 Excel
7. 显示 Excel 路径
8. 打开 Excel 文件

### 7.2 批量匹配页调整

批量匹配页面建议改成：

1. 选择岗位
2. 导入 Excel
3. 显示已导入文件路径
4. 显示可匹配候选人数量
5. 执行批量匹配
6. 显示进度
7. 完成后提示已回写 Excel
8. 提供打开 Excel 文件按钮

### 7.3 删除的 UI 能力

以下能力建议删除：

1. 候选人卡片式库管理
2. 候选人详情右侧管理面板
3. 候选人数据库筛选
4. 候选人来源历史展示
5. HTML 匹配报告展示面板

## 8. 技术实现建议

### 8.1 Excel 库选择

建议使用 `openpyxl`。

原因：

1. 支持 `.xlsx` 读写。
2. 适合逐行写入和后续回写。
3. 易于设置表头、列宽、自动换行、冻结窗格。
4. Python 生态成熟，适合当前桌面程序。

需要新增依赖：

`openpyxl`

### 8.2 Excel 格式优化建议

为了提高可读性，建议在创建工作簿时顺手做基础样式：

1. 冻结首行。
2. 设置表头加粗。
3. 设置适当列宽。
4. 对 `简历详情`、`匹配详情` 开启自动换行。
5. 对 `简历抓取状态`、`匹配度分数` 可选做基础底色标识。

### 8.3 纯文本匹配输出建议

建议把 LLM 输出要求改为纯文本，例如：

1. 输出分数。
2. 输出总体结论。
3. 输出亮点分析。
4. 输出短板分析。
5. 输出建议动作。

程序只做轻量解析：

1. 抽取分数。
2. 保存全文。

## 9. 详细实施顺序

### 阶段一：基础设施

1. 引入 `openpyxl` 依赖。
2. 新建 Excel 服务模块。
3. 定义列结构常量、表头写入、文件创建规则。
4. 实现候选人索引写入。
5. 实现详情回写。
6. 实现 Excel 保存和读取。

### 阶段二：抓取链路切换

1. 移除候选人抓取对 `CandidateRepository` 的主链路依赖。
2. 在抓取开始时创建 Excel 文件。
3. 将结果页候选人逐行写入 Excel。
4. 将详情抓取结果逐行回写 Excel。
5. 在 UI 中显示当前导出文件路径。
6. 增加“打开当前 Excel”按钮。

### 阶段三：批量匹配链路切换

1. 为批量匹配页面增加 Excel 文件导入入口。
2. 从 Excel 中读取所有可匹配行。
3. 将匹配输出改为纯文本。
4. 将结果回写到对应行。
5. 删除 HTML 报告依赖。

### 阶段四：UI 精简与清理

1. 将 `候选人库` 页面改为 `候选人抓取`。
2. 删除候选人管理面板和库视图。
3. 精简批量匹配结果展示逻辑。
4. 删除候选人数据库相关 UI 依赖。
5. 删除 HTML 展示链路。

### 阶段五：遗留逻辑清理

1. 删除或隔离候选人数据库相关代码。
2. 清理不再使用的数据模型。
3. 更新测试。
4. 更新 README 或使用说明。

## 10. 验收标准

改造完成后，应满足以下验收标准：

1. 用户可以从候选人抓取页创建岗位 Excel 文件。
2. 候选人抓取时，Excel 会边抓边写。
3. 程序中断后，Excel 中已写入内容仍然完整可读。
4. 抓取成功、失败、部分成功都能在 Excel 中体现状态。
5. 用户可以手动导入一个候选人 Excel 执行批量匹配。
6. 批量匹配会自动处理所有有简历详情的行。
7. 匹配结果会回写到原 Excel 中。
8. 匹配结果只包含纯文本，不再依赖 HTML。
9. GUI 不再承担候选人长期管理职责。
10. 原候选人数据库链路不再是主流程依赖。

## 11. 风险与注意事项

### 11.1 Excel 作为唯一载体的风险

由于不再使用数据库，Excel 文件即为唯一事实来源，因此必须重点保证：

1. 写入及时保存。
2. 异常中断后文件不损坏。
3. 程序读取和回写逻辑稳定。

### 11.2 年龄字段可能为空

年龄在猎聘页面中未必总是稳定存在，因此应允许为空，不影响抓取主流程。

### 11.3 大文本列会导致文件变重

`简历详情` 和 `匹配详情` 都可能较长，因此需控制单次抓取规模，并在表格格式上优化可读性。

### 11.4 第一阶段不处理去重

根据当前业务共识，本次改造不考虑候选人去重。

即：

1. 不做跨文件去重。
2. 不做全局候选人唯一索引。
3. 不做重复候选人识别与合并。

## 12. 后续可能扩展项

当前方案以快速落地、清晰可用为优先，后续若有需要，可继续扩展：

1. 支持在 Excel 中标记“是否参与匹配”。
2. 支持失败候选人补抓。
3. 支持按 Excel 中空分数行继续补跑批量匹配。
4. 支持单独导出匹配结果汇总表。
5. 支持对 Excel 做颜色标记和条件格式。

## 13. 当前结论

本次候选人链路改造的正式方向确定为：

1. 候选人数据不再以程序数据库为主。
2. Excel 成为候选人抓取与匹配的唯一工作载体。
3. 抓取时边抓边写，先建索引，再补详情。
4. 批量匹配通过 Excel 导入并回写结果。
5. 匹配输出统一为纯文本。
6. UI 回归任务控制，不再承担复杂候选人数据管理。

该方案优先解决的是：

1. 工作流清晰性
2. 结果可读性
3. 人工处理便利性
4. 系统复杂度收敛

后续代码改造将以本文件为执行依据逐项推进。
